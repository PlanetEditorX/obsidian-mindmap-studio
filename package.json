{
  "name": "obsidian-mindmap-studio",
  "version": "1.19.5",
  "description": "Editable local-first .mindmap diagrams for Obsidian with synchronized mind map, outline, article, and continuous-reading modes, read-only locking, global search, themes, rich content, image mirrors, tables, code, and exports.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc --noEmit --skipLibCheck && node esbuild.config.mjs production",
    "release": "npm run verify && node scripts/release.mjs",
    "test": "npm run test:unit && npm run test:regression && npm run test:docs && npm run test:repo",
    "test:unit": "node --test tests/display-mode.test.mjs tests/filename.test.mjs tests/image-host.test.mjs tests/reading-location.test.mjs tests/reading-editor-contract.test.mjs tests/repository-cleanup.test.mjs",
    "test:regression": "node scripts/test.mjs",
    "test:docs": "node scripts/check-docs.mjs",
    "test:repo": "node scripts/check-repository.mjs",
    "verify": "npm run test && npm run build",
    "docs:generate": "node scripts/generate-function-reference.mjs",
    "docs:check": "node scripts/check-docs.mjs"
  },
  "keywords": [
    "obsidian",
    "mind-map",
    "mindmap"
  ],
  "author": "MindMap Studio",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.0.0",
    "builtin-modules": "^5.0.0",
    "esbuild": "^0.25.0",
    "obsidian": "^1.13.1",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "fflate": "^0.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
