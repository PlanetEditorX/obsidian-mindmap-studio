# 安全说明

## 支持范围

安全修复优先应用于最新主分支和最新正式版本。旧版本是否回补取决于影响范围与数据兼容风险。

## 报告漏洞

请使用仓库托管平台的私密安全报告功能，或通过项目维护者公开的安全联系渠道报告。不要在公开 Issue 中披露以下内容：

- 可直接利用的漏洞步骤。
- 图床令牌、Authorization Header 或其他真实凭据。
- 用户仓库路径、私人导图或附件。
- 尚未修复的任意文件读写、脚本注入或请求注入细节。

报告应包含：

- 受影响版本和平台。
- 最小复现步骤。
- 预期与实际结果。
- 数据泄露、文件破坏或远程请求影响。
- 可行的缓解建议。

## 凭据与隐私

图床配置可能包含认证 Header。贡献代码、日志、测试夹具和截图中不得提交真实令牌。发布源码包不得包含 Obsidian 设置数据、个人仓库文件或本地分析缓存。

## 安全边界

- 图床上传端点仅接受 HTTP(S)。
- 自定义 Header 必须是扁平 JSON 对象，并拒绝换行注入。
- 返回图片地址必须是 HTTP(S) URL。
- 删除本地图片前必须确认远程上传成功、当前文档已保存且没有其他导图引用。
- `.mindmap`、Markdown、剪贴板和 XMind 输入必须经过模型规范化。
