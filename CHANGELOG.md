# 更新记录

## 1.46.3

- 第一批大型导图性能优化：实测尺寸重排取消重复碰撞与重复边界重算；碰撞检测利用纵向排序提前终止不可能相交的后续扫描；工具栏每次刷新只遍历一次节点树；分帧渲染优先级预计算排序键，避免 `sort` 比较器重复执行视口相交与距离计算。随机差分验证保持碰撞结果和渲染顺序不变。
- 重构只读模式键盘处理：复制、方向键导航、缩放、适应视图和折叠逻辑从主 `keydown` 分发器拆到独立处理函数，行为保持不变，降低事件处理复杂度。
- 远程图片建议文件名解析移动到可独立测试的纯工具函数；无效 URL 明确回退为 `remote-image.png`。远程网络请求失败仍按原行为抛出错误，不会被错误伪装成文件名回退。
- 当前导图族搜索刷新优先复用已通过 `mtime + size` 校验的搜索索引；父链和子图均只在索引缺失或过期时读取 `.mindmap`，同一轮父级爬升得到的解析结果会在向下遍历时复用，避免重复文件读取。
- 修复旧子导图或异常数据缺失 `navigation.parentPath` 时，导图模式左上角“← 父导图 › 当前导图”返回控件完全消失的问题。
- 打开缺失导航元数据的 `.mindmap` 时，会复用已按 `mtime + size` 校验的全局搜索索引，从父节点 `submap.path` 反向确认真实挂载关系，并恢复 `parentPath`、`parentNodeId`、父标题和来源节点文字。
- 恢复只应用于运行态导航，不进入撤销历史、不制造无内容修改的自动保存；文章族缓存会同步失效并重建。用户之后发生真实内容编辑时，恢复出的导航元数据会随正常保存持久化。
- 新增父级反查、视图恢复、局部面包屑刷新和禁止额外保存的回归契约，并更新父子导图数据模型、架构、特殊功能与调试说明。

## 1.46.2

- 新增文章族上下文缓存：首次构建后将 `baseDepth`、目录、分页导航和通读章节快照保存到插件私有 `cache/article-context-cache.json`，同一文章族未变化时再次打开可在 `setViewData()` 创建编辑器前同步命中，不再重复执行父子导图遍历。
- 新增会话级 `MindMapDocumentCache`：按 `.mindmap` 文件的 `mtime + size` 复用已解析文档，降低重复 `parseDocument()` 开销；缓存对象以克隆形式进出，避免编辑器修改污染后续打开。
- 当前文件编辑、外部修改、删除和重命名会让所有依赖该文件的文章族缓存失效；新建/重命名导图会保守清空文章上下文，防止原本缺失的子导图路径突然可解析。构建期间若发生并发文件变化，本次结果不会写入缓存。
- 修复目录进入正文后必须先手动滚到已加载边缘才继续加载的问题：首次 MISS 仍以目标前后约 5 KB 快速出首屏，真实目标定位稳定后自动按动画帧后台补载后文，再补载前文；每帧最多扩展 4 个约 5 KB 分块，并持续刷新文章缩略导航/滚动高度。向上补载继续补偿新增 `scrollHeight`，用户无需先触碰窗口边缘。
- 缓存命中时首个真实正文窗口预算提升到约 32 KB，再沿用同一后台预热直至前后内容补齐；边缘 `loadBefore()` / `loadAfter()` 保留为用户滚动时的兜底，不恢复旧的节点级 HTML 持久缓存。
- 修复“返回目录再打开仍然 cache-miss”：纯阅读、章节定位、返回目录等只改变内存视图状态时不再调用 `TextFileView.save()` 写回 `.mindmap`；只有真实编辑器 `onChange` 提升文档修订后才写盘并触发正常缓存失效，避免无内容变化的导航产生 vault `modify`。
- 新增自动预热、缓存命中 32 KB 首窗、干净导航禁止写盘和安装 bundle 契约测试，并更新架构、特殊功能、测试及人工验证说明。

## 1.45.16

- 取消内容为空时自动删除导图节点的行为：行内文字清空、完整节点编辑清空、删除最后一个图片/表格/代码/内容块，以及跨节点移走最后一个内容块后，来源节点都继续保留为空节点；只有显式“删除节点”才改变树结构。
- 文章模式行内编辑框增加稳定的一行最小高度；即使内容被清空、浏览器留下 `<br>` 导致 `:empty` 占位规则不生效，标题和正文编辑区域也不会塌缩成难以点击的细条。
- 更新内容块移动与空节点专项测试、README、数据模型、特殊功能和测试说明。

## 1.45.15

- 根据 1.45.14 Windows / Obsidian 1.12.7 日志确认：点击全局搜索结果时第一个搜索 Modal 已执行 `result-close-request → modal-on-close → result-close-return` 且 DOM 已断开，但随后页面仍存在第二个搜索遮罩，手动关闭时再次出现 `modal-on-close(openingResult=false)`；根因是全局搜索快捷键可被插件捕获层与 Obsidian 热键链重复触发，从而叠加两个全局搜索实例。
- 全局搜索入口新增单实例守卫：`globalSearchLaunchPending` 覆盖索引 ready 的异步窗口，`globalSearchModal.isMounted()` 覆盖已打开实例；重复触发只记录 `open-deduplicated`，不再创建第二层 Modal。
- 当前文件/当前导图族搜索继续走独立 `openMapFamilySearch()`，不使用全局单例守卫，保持已经验证正常的自动关闭行为不变。
- 保留 1.45.14 的单次原生 `Modal.close()`、双 RAF 非阻塞导航和搜索后文章行内编辑保护。

## 1.45.14

- 修复 1.45.13 中“程序化 `.modal-bg.click()` 有调试 click 事件但搜索框仍不关闭”的回归：结果打开不再模拟遮罩点击，只调用一次 Obsidian 公开 `Modal.close()`。
- 关闭搜索结果时继续设置 `shouldRestoreSelection=false`，但不修改/删除任何 `.modal-container`、`.modal-bg` 或 Modal DOM，也不等待 `onClose()`；只让出两个绘制帧后开始目标文件/节点导航。
- 新增 `result-close-request`、`result-close-return`、`modal-on-close`、`result-navigation-start` 调试事件，真实桌面端可以直接确认原生关闭请求、宿主回调和导航的先后关系。
- 更新全局搜索/当前导图族搜索关闭契约，明确禁止重新引入合成 backdrop click、PointerEvent、`display:none`、手工 remove 或阻塞式 `onClose()` Promise。

## 1.45.13

- 根据 1.45.12 Windows / Obsidian 1.12.7 日志确认：点击搜索结果后既没有 `open-view-start`，搜索层也仍在前台，说明导航被 `await waitForHostClose()` 卡住，而宿主没有解析本轮等待的 `onClose()`。
- 搜索结果关闭不再等待 `onClose()` Promise；设置 `shouldRestoreSelection=false` 后仅隐藏搜索 Modal 内部内容，并自动触发当前 `.modal-container` 内 `.modal-bg.click()`，复用用户手动点击背景时已验证有效的宿主关闭路径。
- 插件不再修改/删除 `.modal-container` 或 `.modal-bg`；导航只让出一个 `setTimeout(0)` 事件循环后立即执行，找不到背景节点时才回退到公开的 `Modal.close()`，避免再次出现“既不关闭也不跳转”。
- 更新全局/导图族搜索契约与安装 bundle，继续保留搜索后文章编辑的导航事务接管和初始焦点保护。

## 1.45.12

- 根据 1.45.11 Windows / Obsidian 1.12.7 日志确认：搜索结果点击后页面已经开始导航，但搜索结果层和 `modal-bg` 仍可继续接收点击，说明 1.45.11 的 `display:none` 视觉兜底干扰了 Obsidian 原生 Modal 关闭完成流程。
- 搜索结果现在先创建宿主关闭完成 Promise、关闭 `shouldRestoreSelection` 并只调用一次 `Modal.close()`；随后仅使用 `visibility:hidden` 与 `pointer-events:none` 立即隐藏正在关闭的搜索外壳，不再使用 `display:none`。
- 文件/节点导航改为等待真实 `onClose()` 完成后再执行，不再用固定两个动画帧猜测焦点 Scope 已释放；继续禁止手工删除 Modal-owned DOM 和二次 `close()`。
- 更新全局/导图族搜索契约，要求“点击结果立即视觉隐藏 + 原生 Modal teardown 完整完成 + 导航后文章编辑可持焦”三个边界同时成立。

## 1.45.11

- 根据 1.45.10 的 Windows / Obsidian 1.12.7 真实日志确认最终根因：搜索前文章行内编辑可稳定持焦，搜索结果跳转后同一 `contenteditable` 会在获得焦点约 6–8 ms 后持续 `blur(null)`；这不是文章重绘或普通失焦，而是搜索 Modal 关闭后遗留的宿主焦点约束。
- 修正 1.45.3 的搜索弹窗兜底关闭：结果打开时仍同步隐藏 `modalEl`、`containerEl` 与实际 `.modal-container`，但不再手工 `remove()` Obsidian 管理的 Modal DOM，也不在导航完成后第二次 `close()`。
- 关闭 `shouldRestoreSelection`，只调用一次 `Modal.close()`，并等待两个动画帧让 Obsidian 完成 Modal 栈与焦点 Scope 释放后再启动文件/节点导航；`.mms-global-search-container-closing` 继续保证主题残留空壳不可见且不接收指针。
- 更新全局搜索关闭契约，明确禁止绕过宿主 Modal 生命周期；保留 1.45.10 的搜索定位接管和文章指针编辑保护作为独立兼容层。

## 1.45.10

- 根据真实 1.45.9 调试日志修正此前根因判断：问题发生在全局搜索结果跳转后的**直接单击文字编辑**，并非仅由右键菜单关闭回收焦点触发。
- 用户通过指针开始文章行内编辑时现在先调用 `claimInlineEditInteraction()`，立即取消仍存活的语义位置恢复事务和文章窗口边缘扩展，清除待处理文章定位，并提前建立 `inlineEditingId` 保护，避免迟到的搜索定位/上下文刷新替换刚创建的 `contenteditable`。
- 新增 `activateInlineEditableFromPointer()`：保留浏览器按单击位置放置光标，只在最初 120 ms 保护宿主焦点交接；同时增加 `inline-edit-claim/focus/blur/refocus` 调试事件，便于真实 Obsidian 继续定位。右键菜单保护、双击/键盘编辑和正常失焦提交语义保持兼容。
- 新增搜索导航接管专项契约；本地单元测试增至 355 项，其中 345 通过，10 项仅因上传依赖缺少 Linux esbuild 二进制失败。

## 1.45.8

- 修复 1.45.7 在 GitHub Actions `test:regression` 中破坏完整节点编辑契约的问题：`editSelected(initialBlockId?)` 恢复原签名，不再承载文章右键菜单专用焦点参数。
- 新增 `editSelectedFromContextMenu()` 隔离菜单焦点交接；文章模式继续调用 `editSelectedArticleContent(true)`，导图/大纲仍走原完整节点编辑路径，因此保留“搜索跳转后行内编辑不立即退出”的修复。
- 扩展文章上下文菜单与综合回归契约，明确菜单保护不得扩散到完整节点编辑 API。

## 1.45.7

- 修复全局搜索或当前导图族搜索跳转到文章位置后，从右键菜单选择“编辑当前内容/添加正文”时行内编辑器可能立即失焦退出的问题。
- 文章右键菜单启动行内编辑时启用短暂的初始焦点保护，覆盖 Obsidian 菜单关闭阶段的焦点回收；双击正文、键盘快速编辑和正常失焦保存行为保持不变。
- 新增文章上下文菜单焦点交接专项契约，并同步安装 bundle、文档、版本与交接信息。

## 1.45.3

- 修复 1.45.2 中全局搜索/当前导图族搜索点击结果后仍可能只清空内容、空白 Modal 留在前台的问题。
- 两种搜索入口共用 `GlobalMindMapSearchModal`；结果打开前现在同时隐藏 `modalEl`、`containerEl` 与实际 `.modal-container`，执行 `Modal.close()` 后再移除捕获节点，并按 `.mms-global-search-modal` 做 DOM 兜底清理。
- 导航只有在搜索 UI 已同步退出后才开始；保留重复点击保护与导航结束后的幂等二次清理。
- 更新搜索关闭契约测试，并同步安装 bundle、文档、版本与交接信息。

## 1.43.9

- 修复从文章正文切到导图或大纲修改后再返回文章时，旧文章滚动状态抢占当前节点、连续自动加载前文直到页首的问题。
- 返回正文现在会在重绘前把临时模式中的当前节点设为显式文章目标，并保留首次进入顶层文章仍默认显示目录的既有规则。
- 程序化语义定位期间暂停文章窗口边缘自动扩展；用户滚轮、触摸、指针或键盘接管后才恢复正常加载前文/后文。
- 新增文章往返模式定位和恢复期间禁止自动补载的回归契约；单元测试增至 325 项。

## 1.43.8

- 修复 XMind 多画布嵌套导入时，深层同名画布根主题的图片、公式和备注被丢弃的问题。
- 同名挂载主题现在使用根主题的首个标题内容判断是否合并，不再受后续 LaTeX 文字块改变兼容 `text` 镜像的影响。
- 被链接画布根主题除重复标题外的图片、公式、备注和下级主题会完整并入挂载节点；共享图片仍只保存一次并重写全部引用。
- 新增两级嵌套画布根图片、公式、备注及资源落盘回归测试；单元测试增至 324 项。

## 1.43.7

- 修复活动 MindMap Studio 视图中 `Ctrl/Cmd+F` 无法打开搜索的问题；当前父子导图族搜索现在由插件直接接管，不再依赖 Obsidian 的“搜索当前文件”快捷键是否绑定。
- 快捷键在正文、节点编辑框等可编辑区域聚焦时仍然可用，并使用 `KeyboardEvent.code` 兼容非英文键盘布局；弹窗内部不会重复打开搜索。
- 旧版 `Ctrl/Cmd+Alt+F` 保持兼容，可配置的全局搜索快捷键仍优先处理。

## 1.43.6

- 新增“视图与阅读 → 显示右下角加载进度”开关，默认关闭；关闭时只隐藏文章/通读右下角进度，不影响父子导图解析和正文加载。
- 修复“全部替换”只处理界面当前显示结果的问题：现在会重新查询当前全局或导图族范围内的全部匹配，不受“单次最多显示结果”限制。
- 批量替换完成后立即刷新对应文件的本地搜索索引，并保留替换文本的首尾空格；单条替换继续使用同一持久化与校验链路。
- 新增加载进度默认关闭、完整范围替换和索引即时刷新契约测试；单元测试增至 323 项。

## 1.43.4

- 补齐文章上下文进度接口与右下角进度渲染方法的 JSDoc，修复 GitHub Actions 文档覆盖检查因声明缺少注释而失败。
- 重新生成函数参考并验证 56 个源码模块、1127 个具名声明全部满足文档覆盖要求。
- 文章与通读右下角加载进度行为保持不变：持续显示阶段百分比，达到 100% 后自动隐藏。


## 1.43.3

- 在文章模式与通读模式右下角新增非阻塞“加载进度”浮层，按文章族解析阶段实时显示百分比与当前步骤，不会遮挡正文交互。
- 文章族上下文构建现在通过统一进度回调上报“解析结构 → 建立父子导图关系 → 生成目录/章节索引 → 整理导航”等阶段；完成到 100% 后会自动淡出隐藏。
- 失败回退时同样会把进度收口到完成态后自动消失，避免旧进度残留在界面上。
- 新增文章上下文进度 helper、视图回调链路与右下角样式契约测试；单元测试增至 321 项。
## 1.42.11

- XMind 导入新增主题图片识别：解析 `xap:resources/...`、`resources/...`、数组和嵌套扩展字段，将归档二进制保存到当前导图资源目录并写回普通图片内容块。
- 同一 XMind 图片被多个主题引用时只保存一次，所有引用保持各自宽高；缺失资源会在完成提示中报告，不再生成无法显示的临时地址。
- 新增 XMind LaTeX 识别，兼容 `equation/equations/formula/formulas/latex/tex` 及嵌套对象，清理已有美元分隔符后保存为可编辑公式内容块。
- 导入进度与完成提示增加图片提取、公式识别和缺失资源数量；桌面与浏览器文件选择链路使用同一资源保存逻辑。
- 新增共享资源去重、图片尺寸、公式字段、数据 URL 自包含回退和缺失资源专项回归；单元测试增至 319 项。
## 1.41.5

- 修复中心节点将“文章编号方式”设为关闭后，一级子节点仍从“第一章”自动编号的问题；根节点的 `none` 现在作为当前物理导图的整页编号开关。
- 关闭整页编号时，文章正文、递归目录、通读模式、HTML/Markdown/Word 导出及子文章页标题统一隐藏编号，同时保留标题层级、目录结构、分页导航和阅读位置。
- 普通非根节点的“关闭”语义保持不变：只跳过当前节点且不占序号，后代仍按结构层级继续编号。
- “主题与外观”和节点编辑中的说明补充中心节点与普通节点的差异，避免把关闭误解为自动层级回退。
- 新增根节点整页关闭、普通节点单点跳号及目录/页标题共用关闭状态的回归；单元测试增至 294 项。

## 1.41.4

- 为合并回主导图、提取/创建子导图、父子导图导航以及导图/大纲/文章/通读/题库模式切换增加统一页面过渡层；状态层先完成两帧绘制，再启动大文件克隆、序列化、保存和渲染。
- 合并与提取显示分阶段中文状态：读取与复制、更新主导图、打开目标文件；大文件操作期间不再表现为点击后毫无反馈。
- 所有插件内跨导图入口统一经过导航过渡，目标页面挂载后短暂淡入；外部文件切换也在新页面首次挂载时应用入口过渡。
- 页面切换异常由统一导航边界捕获，遮罩会正常退出并显示错误提示，不再产生未处理 Promise 拒绝或永久等待状态。
- 状态层包含 `role=status`、`aria-live` 和 `aria-busy`；系统启用“减少动态效果”时保留文字状态但关闭脉冲、位移和淡入动画。
- 新增大型操作先绘制状态、子导图分阶段文案、全局导航包装、页面入口动画与减少动态效果专项回归；单元测试增至 291 项。

## 1.41.3

- 根据真实 1.41.2 调试日志修复文章表格列宽调整后跳回目录或父级文章：列宽保存不再同步重建整篇文章 DOM，当前章节、目录/正文落点和语义阅读位置保持不变。
- 修复无关内容保存误触发“中心标题同步文件名”：仅当中心标题相对最近一次加载或保存确实变化时才重命名，表格列宽、样式和正文编辑不会因既有文件名与标题不一致而改变物理路径。
- 表格默认固定适配当前文章页面宽度；保存的列宽按比例应用，长内容自动换行，表格外壳不再出现横向滚动条。
- 列宽拖动改为当前列与右侧相邻列反向调整，总宽度保持不变；最后一列不再显示无效的右侧拖动柄。
- 新增列宽边界、百分比渲染、无整页重绘、无横向滚动和标题同步保护专项回归；单元测试增至 290 项。

## 1.40.11

- 删除“加载前文/加载后文”按钮加载状态中的圆角边框跑马灯，包括注册角度变量、`conic-gradient` 遮罩、伪元素和无限循环关键帧。
- 保留按钮 Flex 双轴居中和原有内部流光，文章 5 KB 窗口、边缘扩展与导航行为不变。
- 增加 CSS 契约，明确禁止重新出现边框跑马灯，并继续验证减少动态效果下内部流光停止。

## 1.40.10

- 文章窗口边缘的“加载前文/加载后文”按钮改用 Flex 双轴居中，消除按钮继承行高或内边距后文字视觉偏下的问题。
- 加载进行中时，新增一小段较深的主题强调色沿圆角边框连续运行；原有内部流光保留，已显示正文不被遮罩。
- 边框动画仅在实际加载状态出现，并跟随 Obsidian 主题强调色；系统启用“减少动态效果”时边框跑马灯和内部流光同时停止。
- 增加加载按钮垂直居中、圆角边框跑马灯及减少动态效果 CSS 契约。

## 1.40.9

- 根据真实 1.40.8 调试日志修复“返回目录后立即再次进入子文章”。日志显示目录意图、目录首帧和 `directoryOnly=true` 均已成功，但文章上下文刷新后的 `set-options-restore-choice` 仍选择刚离开的子文件阅读位置，随后主动触发新的 `open-path-request`。
- 文章目录现在是恢复链路的终止落地页：只要当前文章模式正在显示生成目录，异步上下文刷新就不恢复任何章节 `ReadingLocation`，也不允许历史子文件位置触发跨文件导航。
- 新增 `chooseArticleLandingRefreshLocation()` 纯函数，将“目录禁止章节恢复”作为可执行规则；调试事件增加 `articleDirectoryActive`，可直接确认本次恢复是否被目录状态抑制。
- 保留历史阅读位置本身，用户再次主动点击目录章节时仍按精确节点进入正文；仅禁止目录页面刷新自动消费旧位置。
- 新增真实冲突回归：目录激活时返回 `null`，正文激活时仍按“显式目标 → 当前锚点 → 历史位置”恢复。单元测试增至 289 项。

## 1.40.5

- 根据真实 1.40.4 调试日志修复章节目标被根节点覆盖：文章上下文刷新完成前，用户点击目标仍正确保存在 `render-decision.explicitTarget`，但 `setOptions()` 随后优先恢复骨架/历史位置，把 `selectedId` 和待挂载目标改成文档根节点。现在刷新恢复顺序固定为“本次精确节点 → 当前 DOM 锚点 → 历史阅读位置”。
- 修复返回父导图后立即再次打开子导图：父文件上下文完成时，旧子文件阅读位置不再触发 `onDisplayModeChange()` 的跨文件导航；返回目标的父节点 ID 会持续保留到上下文刷新真正消费。
- 视图层新增一次性 `preferredCurrentNodeId`，跨文件章节和父级返回不再依赖可能被异步恢复修改的 `selectedId` 推断目标。
- 文章入口骨架期间，本次渲染捕获的显式章节优先于后来到达的普通恢复请求；真正的新点击会启动新的渲染令牌，不受影响。
- 调试日志新增 `set-options-restore-choice`，同时记录请求节点、骨架节点、历史节点和最终选择，便于直接确认是否仍有目标覆盖。
- 新增两个可执行纯函数回归，直接重放日志中的两种冲突：父节点目标对旧子文件位置、点击章节对根节点恢复。单元测试增至 283 项。
- 注意：本修复使用现有 1.39.13 源码基线合入，并按用户日志中的运行版本 1.40.4 提升为 1.40.5；真实 1.40.4 若另有未提供源码的改动，需要以后续源码包再做无损合并。

## 1.39.13

- 修复返回父导图失效：父级导航在目标节点 ID 缺失、过期或来自旧文件时，会读取子导图来源并按子导图路径在父导图中反查真实挂载节点；找不到时仍打开父导图并给出提示，不再静默停留。
- 修复跨文件章节点击落到页首：显式 `filePath + nodeId` 目标在文章上下文尚未完成时也强制正文落地，优先级高于“显示目录”；文件切换会原子提交新文档与选项，并取消上一文件残留的滚动、动画帧和布局稳定事务。
- 新增“管理配置 → 调试模式”。启用后从本次插件会话开始，在有上限的内存缓冲区记录点击、按键、滚动、文件/视图切换、目录与父级导航、目标排队、文章窗口挂载、语义滚动事务和运行异常；不记录文章正文、输入框内容、图床密钥或 AI 请求正文。
- 命令面板新增“复制 MindMap Studio 调试记录”，将会话元数据和结构化 JSONL 日志复制到剪贴板；剪贴板 API 不可用时使用本地文本框回退。关闭调试模式或重新启动插件会清空会话记录。
- 新增显式目标优先、跨文件旧事务取消、父挂载节点反查和调试日志契约；单元测试增至 281 项。

## 1.39.12

- 文章配置为“显示目录”时，首次打开先等待文章族上下文并直接绘制目录骨架，不再短暂挂载当前文件原始正文后再切换目录。上下文读取失败会以当前文件安全回退并结束骨架，避免永久等待。
- 目录与正文切换统一使用目标类型过渡：目录跳章节显示正文骨架，章节返回目录显示目录骨架；返回目录不再继承离开章节的 `ReadingLocation`，目录稳定从顶部进入。
- 跨文件目录点击会在 `setViewState()` 前排队目标节点，新视图在首次文章上下文刷新前同步消费目标；避免先按父章节层级和子文件根标题生成“第三章 天体运行”，随后再闪烁修正为“第三节 天体运行”。
- 当前文件目录项同样先提交显式目标再刷新文章上下文，过渡、目标窗口和语义定位由同一次导航事务消费，不再产生中间标题或二次滚动。
- 新增文章上下文首帧门、目录双向过渡、目录顶部落地和跨文件目标预排队专项回归。

## 1.39.11

- 修复文章子章节偶发跳到页首：阅读位置采集只接受文章文档标题和真实 `.mms-article-node`，不再把带根节点 ID 的整页容器误记为当前章节。
- 章节定位改为“最后一次导航独占”的语义滚动事务；新点击会取消旧的 20 ms 重试、双动画帧和布局观察，旧目标不能在新目标已到位后再次改写 `scrollTop`。
- 文章上下文异步刷新优先沿用仍在执行的显式目标位置，避免目录落地模式切换、保存刷新或骨架替换重新捕获旧章节。
- 通过 `ResizeObserver` 在最多 5 秒内稳定目标章节，图片、表格、代码或字体晚到导致页面增高时重新校准同一语义锚点；滚轮、触摸、指针或键盘翻页输入会立即取消稳定事务，把滚动控制权交还用户。
- 精确滚动写入增加亚像素阈值，避免布局观察期间因小数舍入反复抖动；新增章节页壳排除、旧任务取消、异步布局稳定和用户输入接管专项回归。

## 1.39.10

- 文章正文首次进入、从目录跳向目标章节或切换到另一篇文章时，先绘制一块固定范围的轻量骨架，再在两个动画帧后挂载真实 5 KB 目标窗口；骨架不覆盖已渲染正文，也不参与章节高度或滚动定位。
- 真实文章页和新增章节使用约 140–150 ms 的淡入/轻微位移动画；向前扩展从上方淡入，向后扩展从下方淡入。
- 滚动到窗口边缘时，“加载前文/后文”按钮先显示非阻塞流光，再执行约 5 KB 扩展；已显示正文保持可读，向上补载继续补偿 `scrollTop`。
- 过渡期间的章节 `ReadingLocation` 会暂存，真实目标窗口挂载后再恢复，避免骨架阶段查询不到节点而回到页首；快速连续导航会取消旧动画帧，只执行最后一次目标。
- 系统启用“减少动态效果”时跳过骨架等待和所有新增动画，直接挂载目标窗口。
- 新增文章入口骨架、双帧绘制门、边缘加载反馈、位置暂存和减少动态效果专项回归。

## 1.39.9

- 大文件文章模式改为目标窗口渲染：首次只挂载点击节点前后各约 5 KB 的真实章节 DOM，接近已加载边缘时再按约 5 KB 向前或向后扩展。
- 向上补载前文时按新增文档高度补偿 `scrollTop`，避免当前阅读段落被新 DOM 顶走；仍不恢复空占位、隐藏舞台、骨架覆盖层或后台全页 hydration。
- 修复外部目录点击同一物理文件章节仍可能停在页首的问题：同文件目录项直接切换到正文并聚焦节点，不再重新打开当前文件。
- 显式章节跳转在本次文章重绘中独占目标 `ReadingLocation`，旧阅读位置不再通过延迟恢复覆盖新目标；定位前会强制把目标节点挂载到当前窗口，并使用精确文章章节选择器。
- 大文档窗口权重改为读取已规范化原始字段，不再为全部节点预先执行内容块规范化和整节点 `JSON.stringify()`。
- 新增 5 KB 窗口、边缘扩展、同文件目录直达、旧位置竞争和目标强制挂载专项回归。

## 1.39.8

- 重构文章模式渲染链路：移除逐帧占位、隐藏舞台、页面覆盖层和节点级文章运行时缓存，改为与通读模式一致的一次性真实 DOM 渲染。
- 文章目录和跨文件章节点击现在在目标文件打开后直接定位真实章节节点，不再对空占位执行滚动，因此不会停在页首。
- “返回上一级”、顶部父级导航和 `Esc` 返回统一携带父导图挂载节点 ID；旧文件缺失 `parentNodeId` 时会根据子导图路径从父导图反查挂载节点。
- 保留文章编号、目录、折叠、缩略导航、行内编辑和语义阅读位置机制；删除旧缓存文件与缓存测试，新增单次完整渲染、无占位跳转和父级挂载节点专项契约。
- 统一版本元数据为 1.39.8，并重新生成生产构建与交付文档。

## 1.39.7

- 尝试通过文章上下文就绪状态避免重复渐进渲染，并将章节跳转接入语义阅读位置恢复；该过渡方案已在 1.39.8 被完整文章 DOM 重构替代。

## 1.39.6

- 修复带圈末端序号进入快速编辑后仍停留在阅读态垂直位置、因编辑框顶部内边距而显得高于正文的问题。
- 只将快速编辑中的圆圈向下补偿现有的 7px 顶部内边距；正文输入框、正文文字位置、水平缩进、圆圈尺寸和阅读态样式均保持不变。
- 新增快速编辑 CSS 契约，校验圆圈补偿量与编辑框块级内边距一致，并禁止该修正规则改动正文布局。
- 统一版本元数据为 1.39.6。

## 1.39.5

- 修复自动对齐的带圈末端序号与正文首字紧贴的问题；此前圆圈虽然与普通圆点正文对齐，但外框宽度占满了整个编号槽。
- 保持带圈正文与普通圆点正文的首字起点不变，将圆圈适度缩小并向编号区内收，在圆圈右侧稳定预留约 0.18em 以上间距。
- 同步调整圆圈垂直位置与边框厚度，保持数字字号、首行基线、两位数显示和多行悬挂缩进。
- 更新 CSS 几何契约，直接校验正文对齐、圆圈与正文间距及编号列偏移上限，并统一版本元数据为 1.39.5。

## 1.39.4

- 修复“自动（与上级标题对齐）”下带圈末端序号整体向左越出正文缩进的问题；带圈编号此前被通用编号样式重置了自动缩进。
- 带圈编号现在与普通末端圆点共用同一标识列：圆圈中心对齐圆点中心，编号正文首字也与圆点正文首字对齐。
- 保留“顶格”模式和普通下一级文章序号的既有布局，不改变圆圈尺寸、字体、字重及多行悬挂缩进。
- 新增标识中心与正文起点的几何契约测试，并统一版本元数据为 1.39.4。

## 1.39.3

- 修复 Windows/Obsidian 字体回退导致原生 `①–㊿` 比正文偏小、偏细并与首行基线错位的问题。
- 文章正文与通读模式从第 1 项开始统一使用当前正文字体的十进制数字和 CSS 圆环；固定编号槽让圆圈与正文首行对齐，换行后正文仍保持整齐缩进。
- HTML 导出同步使用同一圆环结构和字体指标；Markdown 与 Word 等无 CSS 输出继续保留第 1–50 项 Unicode 字符及第 51 项以后的 `◯51` 可读回退。
- 提升文章节点缓存渲染修订，自动丢弃 1.39.2 中保存的旧带圈序号 HTML，避免升级后出现双圈或旧字形。
- 更新带圈序号样式契约与导出回归测试，并统一版本元数据为 1.39.3。

## 1.39.2

- “末端正文标识转序号”新增“带圈数字”样式；达到阈值的同级末端正文可显示 `①、②……㊿`，不再只能沿用上级标题的下一层文章编号。
- 第 1–50 项直接使用 Unicode 原生带圈数字；第 51 项及以后在文章与 HTML 导出中使用 CSS 圆圈包裹普通数字，因此 60、70 以及更大的同级数量都可继续编号，不受 Unicode 单字符上限限制。
- 带圈末端序号独立于文章标题的八级编号边界，即使末端正文位于第 8 级标题下面的第 9 级结构，也可继续生成带圈序号；原“下一级文章序号”行为保持兼容。
- 插件全局设置与当前页面“文章样式”均可选择末端序号样式；正文、通读、缓存、HTML、Markdown 和 Word 导出共用同一编号结果。Markdown 与 Word 对第 51 项以后使用 `◯51` 可读回退。
- 新增 Unicode 1/20/21/35/36/50 边界、67 个同级末端节点、深层结构与导出一致性专项测试；统一版本元数据为 1.39.2。

## 1.39.1

- 修复八级文章编号之后重新循环为 `A.` / `（A）` 的问题；第 9 级及更深节点继续保留结构层级，但不再生成与上级混淆的重复编号。
- 修复第 7、8 级同级节点超过 26 个后从 A 重新开始的问题；字母序号现在按 A–Z、AA、AB 持续递增。
- 文章正文、目录、通读与导出继续共用同一编号解析结果；“主题与外观”帮助文案明确说明八级边界。
- 新增深层 `E. → （A）/（B）` 结构、第 9 级无循环编号及超过 Z 的专项测试；统一版本元数据为 1.39.1。

## 1.39.0

- 文件浏览器筛选改为增量 DOM 处理：MutationObserver 只收集新增子树和 `data-path` 变化，合并嵌套扫描根后仅筛选受影响条目；布局初始化和筛选设置语义变化仍执行完整刷新。
- 移除仓库 `create`、`modify`、`delete`、`rename` 事件中的文件树全量扫描，连续编辑和自动保存 `.mindmap` 不再重复遍历整个文件浏览器。
- 文章缓存写盘改为修订号驱动的单飞循环：活动写入期间的连续更新合并到一份后续稳定 JSON 快照，避免排队写入多个中间状态。
- 文章缓存命中只有在实际改变 LRU 顺序时才安排持久化；重复打开当前最新条目和无改动卸载不再重写缓存文件，同时旧条目的访问顺序可跨重启保留。
- 新增文件浏览器局部扫描契约、缓存干净刷新、访问顺序持久化和运行中更新合并专项测试；统一版本元数据为 1.39.0。

## 1.38.9

- 插件设置持久化改为 35 ms 尾随合并与严格串行写入：快速输入、阅读位置更新、设置分类展开和文章锁状态切换只保存最新快照，避免并发 `saveData()` 乱序覆盖和短时间磁盘写入风暴。
- 设置保存仍保持 `await` 语义；写入期间出现的新变化会在当前写入完成后立即追加最新快照，插件卸载时主动刷新待保存状态。
- 文件浏览器筛选只响应文件浏览器内部或其容器增删的 DOM 变化，文章渐进渲染、弹窗和其他工作区变化不再反复触发全树扫描。
- 文件后缀、隐藏目录和资源目录规则在每轮扫描前只规范化一次，并复用已编译谓词；10,000 条路径专项基准由约 119.6 ms 降至 9.5 ms。
- 只有文件浏览器相关设置语义发生变化时才重新筛选；逗号、大小写、斜杠和排序等格式差异不会造成无效刷新。
- 新增合并写入、失败恢复、单飞顺序、规则签名和编译筛选专项测试；统一版本元数据为 1.38.9。

## 1.38.8

- 设置页一级分类默认全部收起，不再固定展开“主题与外观”；用户手动展开或收起的分类列表会写入插件设置，并在下次打开设置页时恢复。
- 设置搜索只临时展开命中的分类，清空搜索后恢复用户记忆状态，不会把搜索产生的展开动作写入配置。
- “进入文章模式”新增“记住上次文章状态”：文章模式自己的锁定/编辑状态独立持久化，不再受导图、大纲、通读或文件级只读状态影响。
- 文章锁状态只写入插件设置，不写回当前 `.mindmap` 文件；默认锁定和沿用进入前状态两种旧策略继续兼容。
- 新增文章入口策略解析、设置分区记忆和配置规范化专项测试；统一版本元数据为 1.38.8。

## 1.38.7

- 优化大文章缓存命中路径：预扫描只判断指纹并记录可复用条目，不再在首屏显示前同步对所有章节执行 `innerHTML` 恢复、净化和交互绑定；缓存章节与普通章节现在共用渐进帧预算，并优先恢复当前节点关系链。
- 为单次文章渲染增加节点内容块 `WeakMap`，文章标题解析、缓存资格判断、正文渲染和 hydration 复用同一份规范化结果，避免包含表格、图片或富文本的节点被重复转换。
- 缓存节点 hydration 对全部 `[data-block-id]` 一次建索引，再在同 ID 小集合中定位段落、图片和表格；多内容块章节由重复全 DOM 查询收敛为一次线性扫描。
- `buildArticleNodeInfo()` 支持注入主文字读取回调，并合并同级标题/末端正文统计遍历，使文章模式可复用内容块缓存而不改变通读等现有调用行为。
- 新增缓存恢复必须位于帧批次、内容块单次规范化和块元素索引专项测试；统一版本元数据为 1.38.7。

## 1.38.6

- 优化文章节点缓存指纹：只序列化当前节点自身和层级、编号、样式等渲染上下文，不再为每个节点递归包含整棵后代子树；深链文章的缓存指纹计算由近似 O(n²) 收敛为 O(n)，后代局部编辑也不会再无谓清空全部祖先章节缓存。
- 文档缓存摘要改为复用已计算的节点指纹集合，不再额外稳定序列化整份导图文档。
- 修复持久缓存启动预载时 LRU 顺序反转的问题；后续加入新缓存时会优先淘汰真正最久未访问的文件，而不是较新的快照。
- 缓存恢复路径统一规范化 Windows 与 Unix 分隔符，磁盘节点表使用无原型对象，降低异常缓存键干扰。
- 补充 Windows ZIP 在 Linux 下出现 `#Uxxxx` 路径的处理规则：识别 `0x7075` Unicode Path Extra Field 后恢复中文名，并以标准 UTF-8 bit 11 重新打包。
- 新增节点局部指纹、后代编辑隔离和重启后 LRU 淘汰顺序专项测试；统一版本元数据为 1.38.6。

## 1.38.5

- 新增文章节点级持久缓存：首次完整渲染后保存节点静态 HTML、内容指纹和文章展示签名，插件启动时预载最近缓存，未变化文件重新打开时可直接恢复完整正文。
- 文档局部修改后按节点指纹判断复用范围；未变化节点直接恢复并仅重新绑定当前编辑器实例的文字、图片、表格和题目交互，变化节点及因层级、编号或样式变化而失效的节点才重新渲染。
- 缓存采用插件私有 JSON、LRU 数量与容量限制、串行防抖写盘，并在文件删除或重命名时同步清理或迁移；缓存版本、文件路径和展示配置不匹配时自动回退到正常渐进渲染。
- 含异步 Markdown 代码组件的节点暂不持久化，避免旧组件事件和失效宿主引用被恢复；缓存 HTML 恢复前会拒绝危险元素并移除事件属性。
- 新增文章缓存指纹、预载、持久化和渲染契约专项测试；统一版本元数据为 1.38.5。

## 1.38.4

- 修复大型文章渐进加载期间用户向下滚动后被反复拉回顶部的问题。
- 增加渐进文章“视口所有权”：滚轮、触摸、鼠标/触控笔按下滚动条或正文，以及方向键、翻页键、Home、End、空格键触发后，当前用户位置立即优先于打开瞬间的旧滚动快照。
- 用户接管视口后清除待恢复的旧语义位置；后续渲染批次仅补齐内容和维持页面高度，不再写入 `scrollTop`，全文完成时也不会再次恢复旧位置。
- 保留未发生用户滚动时的原有阅读位置恢复能力，并新增渐进加载滚动控制专项回归。
- 统一版本元数据为 1.38.4。

## 1.38.3

- 修复大型文章每次打开前同步计算完整导图布局的问题：编辑器初始化不再执行无条件全树布局，只有真正进入导图模式时才计算坐标。
- 优化深层节点布局：同一次 `computeLayout()` 复用子树高度缓存，避免链式或深层结构反复递归计算导致近似平方级耗时。
- 文章渐进渲染改为首批正文完成后立即显示页面，剩余章节继续按帧填充；首次打开和重新打开不再等待全部节点渲染完成后才显示正文。
- 调整文章分帧预算：首帧优先快速产出可读内容，后续帧提高批量吞吐，同时保留取消、阅读位置恢复、折叠和缩略导航逻辑。
- 新增 1200 层节点布局性能回归，并更新大型文档渲染契约、架构和测试说明。
- 统一版本元数据为 1.38.3。

## 1.37.1

- 修复导图和大纲中粘贴外部图片时仍优先使用旧 DOM 焦点节点的问题；非文章模式现在严格使用粘贴瞬间的当前选中节点，异步保存期间继续锁定该节点 ID。
- “编辑节点内容”弹窗新增“+ 粘贴图片”和图片块内“粘贴剪贴板图片”，并支持直接按 `Ctrl/Cmd+V` 插入剪贴板图片。
- 弹窗粘贴图片会保存到仓库、写入当前正在编辑的节点，并沿用现有自动上传延迟排程；直接读取剪贴板受限时提示使用 `Ctrl/Cmd+V`。
- 统一版本元数据为 1.37.1。

## 1.37.0

- 图片所有候选来源加载失败时，导图、文章、通读和大纲直接显示原始/镜像/本地地址，并提供复制地址操作。
- 修复外部图片异步保存期间目标节点漂移：粘贴开始时锁定节点与内容块 ID，目标消失时停止插入，不再回退到其他节点。
- 修复重新打开多图片导图时自动上传通知循环、界面闪烁和旧快照覆盖风险：任务按文件批处理并串行，结果以图片字段补丁合并到最新文档，只显示汇总通知。
- 隐藏文章上一篇、下一篇、返回上级和阅读完成按钮的浏览器/Obsidian 悬浮提示属性。
- 统一版本元数据为 1.37.0，并恢复规范 UTF-8 中文示例路径。

## 1.35.3

- 图床上传与删除请求统一使用同一份当前请求头，移除独立删除请求头配置，修改 Token 后两条链路同步生效。
- API 连通性上传的 1×1 测试图片在成功后进入 1 分钟延迟清理队列；无删除能力的图床明确提示需要手工清理。
- 删除最后一个图片引用时不再立即删除远程文件，而是持久化等待 1 分钟；到期前撤销恢复或重新引用图片会取消删除，Obsidian 重启后继续恢复未到期任务。
- 文章首次加载骨架按当前文章视图和窗口高度动态生成至少 18 行，并横向铺满可视区域，减少大窗口中只有少量占位行的空白。

- 修复完整“编辑节点内容”面板中 `Ctrl/Cmd+B` 先由 `keydown` 加粗、随后又被 Chromium 的 `beforeinput: formatBold` 二次切换回原状态的问题；增加同一格式事件去重，并使用 `KeyboardEvent.code` 兼容不同键盘布局。
- 导入弹窗的支持格式提示改为“MindMap Studio JSON、思维导图或 Markdown”，不再在入口说明中展示第三方品牌名；现有 `.xmind` 文件兼容能力保持不变。

- 未启用任何有效图床时，图片右键菜单隐藏“上传到图床”；启用后自动显示。

- 修复文章和通读大纲中“同行”图片仍各占一行的问题：连续同行图片进入独立 flex 行，移除单图自动居中占位，两张小图可并排并按空间自动换行。
- 图床预设收敛为 Zipline、ImgBB、Freeimage.host 和自定义，新增图床默认使用 Zipline；旧 Zipline v3/v4 配置自动迁移。Zipline 删除复用当前上传 Token，历史图片缺少文件 ID 时会查询文件列表补回，再调用按 ID 删除接口。
- 修复完整节点编辑器中加粗和文字颜色快捷键被 Obsidian 抢先处理的问题；窗口捕获层、文本框回退和浏览器格式输入事件共同处理，与快速编辑保持一致。
- 图片块新增“与相邻图片同行 / 独占一行”排版，导图、大纲和文章按内容块顺序排列同行图片。
- 图床上传前计算 SHA-256，并按图床复用相同二进制的已有 URL；新增可选删除 API，在删除最后引用时经全仓库检查后同步清理远程图片。
- 完整“编辑节点内容”面板恢复加粗、斜体和下划线快捷键，与快速编辑共用同一配置。
- 图片放大预览窗口缩小为约 `86vw × 82vh`，最大 `1400 × 900 px`。
- 恢复截图并识别倒计时原有的小号胶囊样式和“秒后自动识别”提示文案，仅将小数秒改为整数 `3、2、1`，不改变边框暂停、自动确认或 Esc 退出逻辑。
- 修复截图并识别因空工具栏/空样式栏导致初始化脚本提前中断、从而无法自动确认和无法 Esc 退出的问题：恢复完整控制 DOM，仅以完全透明和禁用指针事件隐藏。
- 截图并识别倒计时改为整数 `3、2、1`；鼠标悬停选区边框、拖动条或缩放手柄时暂停，离开边框或重新框选完成后从 3 秒重新开始。
- Esc 同时绑定宿主窗口和截图 iframe 窗口，任一窗口获得焦点都能取消截图。

- 修复“截图并识别”隐藏工具栏后 3 秒自动完成与 Esc 退出失效的问题：工具栏保留完整 DOM 并以透明方式隐藏，避免初始化脚本访问空节点而中断。
- “截图并识别”覆盖层视觉上隐藏底部标注与输出工具栏，但保留完整控件树；普通“截图”继续正常显示完整工具栏。
- 删除截图工具栏中的“固定”功能，并移除 Windows WinForms 与其他平台弹窗固定实现。
- 截图编辑器默认选区改为当前屏幕完整范围；切换“全部屏幕 / 屏幕 N”后同样自动选择对应完整范围。

- 修复截图文字工具在同一次鼠标按下与释放之间立即失焦、导致无法输入的问题：输入框改为稳定延迟聚焦，移除自动失焦提交，并完整拦截编辑期间的键盘与输入事件。
- 箭头工具新增“箭头 / 直线”样式切换，两种样式共用颜色和细中粗线宽。
- 修复 Windows PowerShell 5.1 读取无 BOM 脚本时显示器名称乱码：脚本统一写入 UTF-8 BOM，截图编辑器使用本地生成的“屏幕 N”名称。
- 修复双显示器只能截取 Obsidian 所在屏幕的问题：Windows 改为抓取完整虚拟桌面，并在截图编辑器提供“全部屏幕”和各显示器切换。
- 修复高 DPI 或窗口宽高比不一致时截图背景被放大、拉伸和导出范围偏移的问题：启用 Per-Monitor V2 DPI 感知，背景按比例适配，选区按虚拟桌面原始像素导出。
- 完善标注样式：几何图形支持矩形、圆角矩形、椭圆和半透明填充，画笔、箭头、文字与序号支持颜色和线宽；文字改用画布内输入框，支持中文与多行输入。
- 优化选区视觉，改用圆角细描边、柔和阴影和圆形缩放手柄，降低边缘生硬感。
- 将插件设置“图片识图与本地 OCR”更名为“截图与识别”，并调整为紧随“AI 助手”的独立一级分类。
- 拆分“截图”和“截图并识别”为两条独立命令、独立编辑器快捷键、独立顶部工具栏按钮，不再使用“截图后自动识图”开关。
- 节点右键菜单新增“插入截图”和“插入截图并识别”，截图开始前冻结目标节点或内容块，避免截图期间焦点变化导致插入位置漂移。
- 新增桌面截图编辑覆盖层：支持拖动选区、八方向缩放、全局坐标与尺寸显示，以及几何图形、画笔、箭头、文字、序号、马赛克和橡皮擦标注。
- 普通截图选区默认常驻且不会自动关闭，双击选区后复制并插入节点；截图并识别在选区拖拽或边框调整释放后等待 3 秒自动完成，选区内鼠标活动和继续调整边框会重置计时。
- 修复真实 Obsidian 中高级截图路径不可达后静默进入系统截图、导致无边框、无工具栏、普通截图松开即关闭和截图并识别无延迟的问题：改为本机非交互式整屏抓取加插件覆盖窗口，受限时使用 Obsidian 内嵌覆盖层；禁止静默回退系统交互式截图，失败时给出明确错误。
- Windows 在无法取得 Electron 主窗口句柄时，可由 PowerShell 抓屏脚本临时最小化并恢复前台 Obsidian 窗口；Electron 与 Node.js API 继续按需动态获取。

## 1.35.2

- 收起所有节点后，在紧凑布局稳定后自动执行一次适应画布。
- 适应画布改为平滑缩放和平移，并遵循系统“减少动态效果”设置。
- 全局设置的外观分类改为“主题与外观（全局默认）”，与当前页面“主题与外观”明确区分作用域。
- 统一全局与页面外观中的“主题模板”命名，并调整默认设置分类顺序。

## 未发布

### 优化

- 文章模式重建改为双缓冲过渡：当前页面保持可见，新页面在隐藏渲染区完成后原位淡入替换；首次进入显示静态文章骨架和加载提示，不再先清空容器造成白屏或整页闪烁。加载期间不再把整个文章区域切换为 `progress` 光标，避免鼠标停留时光标反复闪烁。
- 大型导图改为渐进挂载节点 DOM：完整布局仍一次计算以保持连线和坐标一致，但渲染顺序优先当前节点、兄弟节点、父节点及其兄弟，再处理当前视口、相邻视口和其余节点；每批限制在短帧预算内，减少进入页面或结构重绘时的长任务阻塞。
- 导图、文章和大纲中的纯文字提交改为局部刷新，不再因一次 Enter 或失焦保存而重建整张导图或整篇文章；撤销、自动保存、内容块权威字段和跨模式同步保持不变。
- 进入大型文章模式时先提交按钮激活与加载反馈，再建立轻量章节占位并分帧填充内容；优先渲染当前节点关系链，完成后恢复语义阅读位置、章节折叠、回到顶部按钮和缩略导航图。加载动画遵循系统“减少动态效果”设置。

### 修复

- 修复搜索结果点击后目标页面已跳转、但前台搜索弹窗只被清空而没有自动隐藏的问题；结果导航现在会先同步隐藏并移除搜索弹窗容器，再执行跨文件或节点定位。
- 修复 OpenCode Zen 等未开放渲染器跨域读取的 OpenAI 兼容接口在导图右键询问 AI 时提示 `Failed to fetch` 的问题：流式请求遇到浏览器网络/CORS 边界后，会使用同一 API 密钥和请求头通过 Obsidian 原生请求完成，并继续解析 SSE 结果。
- 工具栏“主题与外观”中的背景、图案、节点、文字、边框和连线颜色改为紧凑的圆形色点，不再横向拉伸成长条。
- 新增当前脑图外观颜色控件的样式回归测试，约束圆形尺寸与浏览器色板内层样式。
- 重做文章目录页视觉：改用纸张色渐变、左对齐标题、轻量目录行、层级圆点、悬停箭头与移动端紧凑布局，减少大块灰色卡片感。
- “卡片 / 简洁 / 引导线”继续保留差异，并补充键盘焦点、返回目标高亮和现代目录样式回归测试。
- 统一工具栏“主题与外观”的全部颜色选择器：画布、节点、连线、正文、标题、强调色、纸张背景和末端正文标识颜色均使用固定 30×30 正圆色点。
- 工具栏末端正文标识颜色移除“标识颜色来源 / 跟随插件设置”复选项，改为与设置页一致的“跟随主题”按钮，并保留自定义颜色覆盖。
- 目录样式新增“最初样式、极简书页、现代报告、杂志索引、层级树线”手动选项，并继续保留卡片、简洁和引导线兼容值。
- 所有目录样式彻底移除章节前圆点；目录编号与标题拆分渲染，杂志索引固定为每个目录项独占一行。
- “设置 → 主题与外观”和工具栏“主题与外观”现均提供目录样式选择；当前页面可跟随插件全局设置或单独覆盖。
- 修复大型文章在页面中部输入文字、调整节点或触发异步文章上下文刷新后跳到顶部的问题：重建期间保留旧滚动范围，每批章节挂载后持续校正语义锚点；仅更新跨文件阅读快照的上下文刷新不再重建当前文章。
- 修复图片镜像容灾成功后仍反复提示“图片地址失效，已自动切换”的问题：自动切换现在通过权威 `content` 图片块写回新的主地址，并同步旧版 `node.image` 镜像；后续重绘和重新打开文件不会再恢复到失效地址。
- 修复导入图片自动上传任务在保存前触发“中心节点标题同步文件名”时静默失效的问题：排程现在持有可随重命名更新的 `TFile` 对象，保存完成后按新路径继续读取和写回导图。修复点击节点后画布因无实际尺寸变化的 `ResizeObserver` 通知反复执行二次布局、导致整张导图持续下移的问题；只有节点外框尺寸真实变化时才重新计算碰撞布局。
- 修复桌面 Markdown 图片已复制但节点仍保留原 `assets/...` 引用的问题：导入现在把新路径写回权威 `content` 图片块并同步 `node.image` 兼容字段。导入为子节点、替换整图及 Markdown 转脑图完成后，会为已经复制到当前导图资源目录的图片安排既有图床自动上传流程；新建资源优先按完整仓库路径直接解析，不依赖元数据缓存即时完成索引。
- 修复导图节点编辑结束后立即在空白处执行“收起所有节点”时，延迟的文章上下文刷新又展开原焦点祖先链、导致只收起局部分支的问题；导图模式的普通配置刷新现在保留当前折叠结果，不再把旧阅读位置当作导航恢复。
- 修复粘贴图片已经写入节点后仍偶发弹出“保存同步出现异常”的误报：图片块一旦完成模型写入，会继续显示并执行自动上传排程；后置保存通知或重绘的同步异常改为后台重试，不再弹窗打断已成功的粘贴。本地资源保存、节点插入和自动上传排程仍分别判定。
- 修复表格编辑或调整列宽后视图短暂跳动：文章与大纲在重绘同一帧同步恢复阅读锚点，导图在初次重绘和真实尺寸二次布局时保持当前表格节点的屏幕位置。
- 调整末端正文自动对齐：标识现在悬挂在左侧，正文文字与上级标题文字起点对齐，避免在启用标识时额外向内缩进。
- 统一末端正文样式：原“末端正文圆点/项目符号”更名为“末端正文标识”，新增独立的顶格或自动缩进对齐方式。标识、样式、颜色和对齐方式同时提供插件全局默认与当前页面“文章样式”覆盖，页面设置优先。
- 修复文章模式最上方文档主标题无法右键询问 AI 的问题：右键主标题现在发送当前物理页面的全部内容；下级节点右键仍仅发送该节点及其全部子节点。
- AI 助手改用兼容 SSE 的流式请求：问答、整理和题目整理会即时显示模型思考与生成内容，最终结果仍沿用原有 Markdown 渲染、预览确认和错误处理；默认操作改为“AI 整理并重新生成（确认后应用）”。

- AI 助手弹窗新增与接口配置同步的“深度思考”开关；切换立即保存，并在更换弹窗内接口时显示对应接口的独立状态。
- 修复“获取模型”成功后仍停留在预设默认模型的问题：模型目录现在随接口配置持久化缓存，重新打开设置可直接使用；首次预设、空值或已失效模型会自动切换到新列表首项，手动有效选择保持不变。更换地址或预设会清除旧缓存。
- AI 接口配置现在可持久化选择自动、开启或关闭思考；自动保持服务端默认，显式选择时按 OpenAI/FreeLLMAPI、DeepSeek、硅基流动的兼容协议发送相应控制字段。新增“获取模型”按钮，可从已配置接口的 `/models` 目录读取模型选择建议，不会覆盖当前模型。
- 修复文章中的圈号编号（如 `④`）因单独加粗而显得与周围文字不一致的问题；编号现在继承所在标题或正文的字体、字号、字重和行高。文章行内编辑改为 Enter 保存、Shift+Enter 插入换行，并保留输入法组合输入保护。
- 统一内容删除后的空节点清理：除首次新建且尚未输入内容的节点外，删除最后一个文字、图片、表格或代码块后，真正空白的非根叶节点会自动删除；带子节点、备注、链接、子导图、图标、标签、题目或任务的节点保持不变。
- 修复文章编辑模式删除节点全部文字后按 Enter 又恢复旧文字的问题：清空最后一个文字块时现在通过权威内容块替换同步清除 `text/richText` 兼容镜像。修复行内编辑时截图快捷键被编辑状态提前拦截的问题；已配置的截图快捷键现在可在正文、标题输入过程中正常触发。修复插入或编辑表格后异步文章上下文刷新重绘页面并回到顶部的问题；刷新会恢复当前可见的语义阅读锚点。
- 修复导图模式无法把内容块投放到空节点的问题；空节点现在以整个节点作为末尾投放区域。导图和文章编辑模式统一使用左侧手柄鼠标拖动，可在节点内排序、跨节点精确放到块前后或追加到空白区域；文章原有的移动按钮与方向键移动模式已移除。跨节点移走最后一个块后，真正空白的非根叶节点会自动删除，带子节点或附加信息的节点仍保留。导图表格继续只在双击时打开编辑器。
- 修复 Markdown 导入生成的标题节点首次在文章模式编辑时，因文字块 ID 尚未持久化而把修改结果追加成第二个文字块的问题。导入现在直接生成稳定的文字内容块；旧文件若仍只有 `text/richText` 兼容字段，首次编辑也会覆盖原文字块，不再重复追加。
- 修复文章页面从阅读状态无重绘切换到编辑状态后，表格仍沿用首次渲染时的只读快照，导致双击编辑和列宽拖拽均无效的问题；表格交互现在读取实时锁状态，拖拽手柄会预先渲染并在编辑模式显示。
- 修复文章主标题输入后按 Enter，标题下方又新增一个空白文字节点的问题；行内编辑现在独占 Enter 和 Escape，编辑生命周期内不会继续触发“添加同级/子节点”等结构快捷键。
- 修复文章编辑模式中空白文档主标题失焦后点击区域消失、无法重新进入编辑的问题；主标题现在始终保留可点击占位区域。修复文章表格双击无响应的问题，双击表格可直接打开单元格编辑器。
- 修复文章编辑模式中后续文字块点击后立即丢失编辑状态、双击无响应，以及右键“在此块后插入文字”后新块未保持光标的问题；所有文字块现在按块 ID 进入编辑，并在菜单关闭造成的短暂失焦后恢复到新块末尾。
- 修复 Windows 检出为 CRLF 换行时，`node-creation.test.mjs` 无法提取文章空节点渲染分支而误报失败的问题；源码契约测试现在统一规范化换行符后再断言。
- 修复文章模式在代码块上右键插入文字后，光标错误跳回第一个文字块的问题；新文字块现在插入到右键内容块之后，并精确聚焦该块。
- 文章正文文字块右键新增段落缩进切换，可在默认首行两格与顶格之间切换，并按文字块持久化到文章和通读模式。

### 新增

- 导图画布空白处右键新增“上传当前页面所有图片”：只选择一次图床，跳过已经覆盖全部所选图床的图片，并将可读取的本地或远程图片依次补传到缺失图床；上传结果统一写回远程镜像列表和当前显示地址。
- 新增“末端正文标识转序号”开关与 1–20 的兄弟节点阈值设置（默认 4）；同一上级下达到阈值的末端正文会在文章与通读模式使用下一层序号，3 个节点、最末级或未达阈值时保留原标识。插件全局设置与当前页面文章样式均支持覆盖，页面设置优先。

- 文章编辑工具栏新增“作为块移动”和“作为节点移动”。前者把当前正在编辑的内容块追加到点击的目标节点末尾，并沿用空来源叶节点清理规则；后者把当前节点完整插入到目标节点之后，固定保持同级顺序，例如 `A、B、C` 中把 `C` 移到 `A` 后得到 `A、C、B`。选择目标期间会显示可用范围和操作提示，`Esc` 可取消；根节点、自身和后代等非法节点目标会被拦截。
- 节点编辑器现在使用 Enter 保存并关闭，Shift+Enter 在文字块或备注中插入换行；中文输入法候选确认不会误关闭窗口。导图内容块增加独立拖动手柄，可在节点内部排序，也可把文字、图片、表格或代码块移动到另一节点。右键内容块统一提供“删除当前块”，只删除鼠标所在块。
- AI 问答、整理、题目整理和图片识别请求新增动态进度条、当前处理阶段和已等待秒数；非流式接口不显示虚假百分比，等待超过 30 秒时会提示长内容或模型繁忙可能需要更久。
- 文章编辑模式的表格支持拖动表头分隔线调整列宽；列宽以 `columnWidths` 写入表格内容块，重新加载后自动恢复，并同步用于导图和大纲渲染。代码语言列表新增 Nginx；“节点编辑器显示位置”迁移到“视图与阅读”。
- 插件设置中的“资源文件夹”“在文件浏览器隐藏资源文件夹”和“文件浏览器自定义筛选”统一归入“文件与资源”；“连线与分支”新增全局“分支外观”，可作为当前脑图未单独覆盖时的默认值。当前脑图“主题与外观”按主题模板、画布与字体、节点与文字、连线与分支、文章编号与目录、页面代码设置重新分组，并优化宽屏与窄屏排版。
- 进一步加宽“主题与外观”弹窗，并改用两列独立纵向卡片流，消除卡片高度不一致产生的大块留白；文字样式选项允许换行，避免按钮文字溢出。
- 答题页新增标签筛选；题库文件夹支持每行配置多个路径。错题本新增可选记忆曲线与“答对多少次后移除”阈值，开启后答对的错题会保留至达到掌握次数。
- 文章编辑区的节点右键菜单现在与导图节点一致，可直接使用编辑、插入内容块、题目、代码、表格等操作。

- AI 助手新增“整理为题目节点”操作：模型仅返回题干、选项、答案、解答和标签 JSON；右键节点范围会更新该节点，当前页面范围会在选中节点下新增题目子节点。

- 题库文件夹中的“题库”整页模式更名为“答题”；答题顺序默认随机，可在设置中切换为按导图顺序，题目标签补充常识判断、时政、政治、经济、法律、人文历史、地理科技、公共基础知识等常用分类。
- 题目节点新增判断题：固定提供“正确 / 错误”选项，AI 识题、节点摘要、文章/通读展示和题库自动判题均已支持，并兼容 `A / B` 及“正确 / 错误”参考答案。
- 任意文字输入、粘贴、导入及文章/通读编辑中的标准 Markdown 超链接（如 `[搬瓦工 CN2GT](https://v2rayssr.com/bwgvps/)`）现在会自动识别为可点击链接；编辑时不会误跳转，导出仍保留标准 Markdown 写法。
- 导入入口改为“导入文件”，支持选择“导入为子节点（默认）”或确认后替换当前整张导图。
- 文章编辑模式的代码块支持双击原地编辑；失焦或 Ctrl/Cmd+Enter 保存，Esc 取消。
- 快速行内编辑的选中文字工具栏新增“清除格式”，可一次移除加粗、斜体、下划线、颜色等全部字符样式。
- 图片右键菜单新增左/中/右对齐、预设与自定义尺寸、预览、复制地址、编辑、删除；本地图片可直接选择图床上传，并保留本地地址和已有镜像作为回退。
- 全局搜索结果中的“替换此节点”改为居中的大按钮，鼠标和触屏都可直接操作。
- Markdown 网页剪藏导入支持 YAML 标题、普通图片与带外链图片，并将表格、图片保留为可编辑内容块。

- 生产构建不再将 inline source map 写入 `main.js`，显著降低安装包体积；生成文件冲突可在源码合并后通过重新构建解决。

- 全局代码设置改为可配置行数阈值：不超过指定行数保持展开，超过指定行数自动显示行号；节点代码显式设置仍可覆盖。

- 代码显示设置支持三级优先级：节点代码设置优先，其次是当前页面“主题与外观”中的页面代码设置，最后为插件全局代码设置；未设置时自动跟随上级。

- 代码块新增显示设置：支持默认折叠、行号，以及 Obsidian、GitHub、Monokai、Dracula 样式；导图、通读和大纲视图保持一致。

- 题目节点新增学习状态与练习记录；当前导图可打开题库面板，按关键词、标签和状态筛选，并可快速定位、标为错题或掌握。

- 新增可选题目节点：支持选择题和大题、图文题干/选项/答案/解答、常用考试标签，以及从题图使用 AI 回填题目结构。
- 题目节点新增“AI 转题并检索原题”：可先将题图或文字转为结构化题目；只有返回可验证原题来源时才自动补齐答案与解析。
- 题目节点在导图中直接显示选择项、答案、解答和原题来源；图片右键可转为题目节点并自动智能处理。未找到原题时会改由 AI 分析补齐缺失答案与解答。
- 文章和通读模式现在同步显示题目节点的选择项、答案、解答及原题来源。
- 题目节点的答案与解答默认折叠，可按需显示；选择题选项改为逐项独立显示。

### 修复

- 优化文章编辑模式的节点快捷操作：非根节点下方改为“添加同级、添加子节点、删除、更多”，根节点仅保留“添加子节点、更多”；原“完整编辑”不再常驻，完整属性与内容块编辑移入“更多 / 右键 → 节点设置”。
- 修复新建空白子节点仍处于行内编辑状态时点击“删除节点”无效的问题：按钮在执行前不再触发失焦重绘，删除操作直接绑定节点 ID，并忽略已删除编辑器迟到的 blur 保存。
- 修复设置布局测试直接匹配 `main.js` 中文明文、在 esbuild 将中文序列化为 `\uXXXX` 时误报失败的问题；测试会先生成仅用于断言的可读视图，不修改实际构建产物。
- 修复文章编辑模式右键“编辑节点”无响应的问题：可编辑文字时改为“编辑当前内容”并直接聚焦行内编辑；仅含图片、表格或代码等内容时显示“添加正文”并创建临时正文输入行；文章阅读模式不显示编辑入口，导图模式继续使用完整节点编辑器。
- 新建子导图恢复标准初始结构，默认保留“主题 1”和“主题 2”两个子节点。
- 修复文章编辑模式中右键添加空白子节点后没有可聚焦编辑行、无法直接输入的问题；空白末端节点现在会渲染占位编辑行并自动获得光标。
- 文章空节点占位逻辑仅对“完全没有内容块”的新节点生效；仅含表格、图片、代码等内容的文章节点不会再被误判为需要空正文占位。
- 导图节点增加 36px 全局最低高度，布局测量和运行时 DOM 使用同一高度下限，空白节点不再塌缩为细长条。

- 答题模式的题目解析改为“前置说明一段、`A项 / B项 / C项 / D项` 各一行、D 项后的总结与最终答案一段”；“综上所述，正确选项为……”会完整独立显示，不再并入 D 项或继续拆散。
- 分支外观更名为“圆润卡片分支（曲线）”与“圆角分支（折线）”；未手动覆盖连线类型时会随分支外观使用对应曲线或折线。

- 修复答题页重复显示同一题干的问题；题型与练习状态改为分离标签，解析中的 A/B/C/D 项自动按项分行显示。
- 修复题库选择项虽然写入状态却没有可见选中反馈的问题；改为整行可点的原生单选/复选控件，并在最后一题结束本轮答题而非循环回首题。
- 修复题库模式提交选择题后被阅读态保护拦截、无法保存作答结果的问题；练习状态现在可受控写入次数、正确次数、错题状态和练习时间，而不会开放导图内容编辑。
- 图片预览窗口改为接近屏幕比例的固定画布，移除窗口与图片舞台的滚动条。
- 修复导入后含多个代码块的高节点在真实高度测量阶段被碰撞避让推到前一个兄弟节点上方的问题。
- 修复平衡导图布局按子树高度重排根节点分支的问题；导图、文章和通读现在统一遵循用户拖放后的节点顺序。
- 修复“展开/折叠全部”被连续点击时先收起又立刻展开、仅闪烁而无结果的问题；状态已一致时不再触发无效重绘。
- 快速编辑的清除格式移至工具栏末尾，并补齐导图内快速编辑入口；颜色按钮会显示上次所选颜色，自定义色块恢复为完整圆形。
- 粘贴混合文字与 fenced code 时，现会保留代码前后的文字并按原顺序写入节点内容块。
- 修复图片右键调整对齐、尺寸和上传图床后未写回节点内容的问题；图片预览窗口同步加宽并提高可用显示区域。
- 导图的拖放移动与收缩/展开现在会平滑过渡到新的节点布局，连线同步淡入；遵循系统“减少动态效果”偏好，图片和内容块异步测量仍保持即时更新。
- 修复导图中代码块折叠后节点仍保持展开高度的问题：节点 DOM 不再把布局估算高度写成 `min-height`，仅保留 36px 全局下限和用户手动设置的更高最小高度；代码高亮完成及 `<details>` 展开/折叠时会重新测量节点并刷新碰撞布局与连接线。

- 修复“编辑节点内容”删除表格块或代码块后，保存关闭仍恢复原内容的问题：新增权威内容块替换入口，在同步派生字段前清理旧版 `table`、`code` 等镜像字段，避免兼容迁移逻辑把已删除块重新补回。

- 移除 README 中的当前源码版本硬编码；版本单一事实源改为 `package.json`、`package-lock.json`、`manifest.json` 与 `versions.json`，仓库检查仅禁止重新引入 README 版本文本，避免每次版本升级重复触发 CI。

- 修复发布版本升级到 `1.24.5` 后 README 仍显示旧版本、导致仓库版本一致性测试失败的问题；源码版本、插件清单、锁文件与版本映射现保持一致。

- 完全重构代码块行号实现：移除 `code::before` 多行伪元素和固定光学基线补偿，改为与高亮代码并列的真实 DOM 行号栏；行号与代码共享运行时捕获的字体、字号、字重、行高、字距及上下内边距，并由同一渲染器覆盖导图、大纲、文章和通读模式。

- 修复源码包中的示例导图仍使用 `#Uxxxx` 编码路径的问题；示例文件及父子导图目录现统一使用可读 UTF-8 名称。
- 修复 README 当前源码版本与 `package.json` 不一致的问题，并新增仓库检查防止版本说明再次漂移。

- 修复自动行号阈值在未超过阈值时仍回退为全局显示的问题。

- 修复输入首个字符时文章上下文刷新重绘导图、导致 Tab / Enter 新建节点行内编辑失焦的问题。
- 修复截图前隐藏 Obsidian 在新版桌面运行时无法取得窗口句柄的问题；空节点截图识图会填充原有空文字块。
- 修复 Tab / Enter 新建节点后行内编辑容易在首帧失焦退出的问题；多内容块节点现在按双击位置编辑对应文字块或打开对应图片块。
- 图片识图转文字后，节点默认采用左对齐。
- 修复导图节点文字与子导图节点悬浮时仍显示原生黑色提示的问题。
- 修复导出通读文档时，父导图中已链接到独立子导图的节点会在正文重复生成标题的问题。
- 修复 Markdown 文档导出显示 HTML 锚点标签且目录无法跳转的问题；现在使用普通 Markdown 标题和链接。
- 修复 Word 文档导出目录锚点兼容性，并将 PDF 导出改为桌面端直接生成文件，不再打开 Obsidian 会拦截的打印窗口。
- 修复 Word 文档导出目录的章节顺序与缩进：子导图小节现在紧跟所属章节，并使用嵌套列表保留层级。
- 修复 Word 导出目录无法跳转：Word 导出改为原生 .docx，目录使用内部书签超链接。
- 修复 Word 拒绝过长且含连字符的书签名导致目录无法跳转；书签现在使用合法、短且唯一的 Word 专用名称。
- 修复 Word 导出将同级末端节点误判为标题的问题；末端节点现在统一导出为普通正文，不进入 Word 目录。
- 修复文档导出目录过深、HTML 自动序号和子导图目录项跳转到目录页的问题；目录层级现在跟随文章目录最大层级，子导图项会跳到实际子页章节。
- 修复 Markdown 文档导出目录锚点无法跳转到具体标题的问题。
- 修复 HTML、Word、PDF 和 Markdown 文档导出丢失目录跳转的问题；导出目录现在会链接到对应标题锚点。
- 直接确认识图结果时不再闪现确认预览窗口，单图和批量识图都会在后台原位替换。
- 隐藏导图节点文字和备注触发的原生黑色悬浮提示，保留可访问性标签和可点击控件提示。
- 修复切换到“AI 整理并重新生成”后仍显示询问模式默认问题的问题；现在会加载专用整理要求，并分别保留询问和编辑模式的输入草稿。
- 修复 AI 窗口不同操作模式的字段同时显示问题：本地文字替换只显示“查找文字”和“替换为”，询问与 AI 编辑模式隐藏替换字段。
- 修复 AI 回答 Markdown 渲染将 `AiAskModal` 错误作为 Obsidian `Component` 传入而导致 TypeScript 构建失败；改为使用独立生命周期组件，并在窗口关闭时释放。
- 修复综合回归仍按旧源码顺序检查行内编辑的问题；现在分别验证指针激活委托、共享激活方法和失焦释放，不再因方法抽取产生误报。
- 隐藏文章和大纲文字悬浮时的“章节标题”“正文段落”等编辑提示；辅助标签仅在实际进入行内编辑时启用。
- 修复启用四模式阅读位置同步后，点击节点可能在父子导图或滚动锚点之间反复跳转的问题。
- 程序主动恢复滚动位置时暂停阅读位置采集，阻断“恢复 → scroll 事件 → 保存 → 再恢复”的反馈环。
- 文章、大纲和通读中的节点点击保存其当前视口比例，不再把节点强制写成固定的 35% 锚点。
- 显式打开父导图或子导图时，以用户刚打开的文件为准；旧的跨文件阅读记录不会立即把视图跳回上一文件。

### 功能

- 设置分区按通用使用顺序排列，“管理配置”固定在最后；截图快捷键改为按键录制，点击后直接按下组合键即可修改。
- 设置页新增“管理配置”分类，集中提供配置 JSON 导入、导出与恢复初始配置，不影响导图文件。
- 文档导出统一以通读内容作为 HTML、Word、PDF 和 Markdown 的导出基准。
- 设置页图床配置新增“加载优先级”；节点图片加载和点击放大时按优先级尝试图床地址，失效后自动尝试下一个地址。
- 导出文档在桌面端优先打开系统保存位置选择器；无法打开选择器时默认导出到桌面，非桌面端继续回退到仓库内导出。
- AI 助手新增“图片识图”操作，按节点树顺序识别当前页面或右键节点子树中的全部图片，并汇总成功与失败结果。
- 图片右键新增 AI 识图/本地 OCR；先显示原图与可编辑识别文字对比，取消不修改，确认后在原内容块位置替换并进入撤销历史。
- 工具栏和命令面板新增截图插入，默认快捷键为 `Ctrl/Cmd+Shift+S`；可设置截图前最小化 Obsidian、截图后恢复、按截图前焦点插入节点或文章段落、无焦点时仅保留系统剪贴板。
- 截图可选自动运行当前识图方式并打开确认预览。
- 设置新增 AI 视觉识图与本机 Tesseract OCR 模式切换；本地 OCR 通过 `execFile` 调用且不经过 shell、不上传图片。
- AI 助手新增“AI 整理并重新生成”模式：模型返回 Markdown 修改提案，插件先解析和预览，用户确认后才替换当前页面或右键节点子树，并写入撤销历史。
- AI 节点编辑保留被替换范围根节点的稳定 ID、样式、子导图引用、链接和文章编号元数据；预览后内容变化时拒绝应用旧提案。
- 新增完全离线的本地文字替换：支持当前页面或节点子树、大小写选项、命中预览和确认应用，不修改链接、代码、图片地址或子导图路径。
- AI 编辑输出增加一级标题、2 MB 和 5000 节点硬限制，显式只读锁定时禁止应用。
- 新增硅基流动 AI 预设，默认基础地址为 `https://api.siliconflow.cn/v1`，并提供 `deepseek-ai/DeepSeek-V4-Flash`、`deepseek-ai/DeepSeek-V4-Pro`、`zai-org/GLM-5.2` 模型建议。
- 新增 FreeLLMAPI 预设，接口地址默认留空，模型默认为 `auto`，适配用户自行部署的 OpenAI 兼容路由。
- AI 接口卡片新增“检测接口”按钮；检测仅发送最小 `OK` 提示，不包含当前导图或节点正文。
- AI 地址同时接受 `/v1` 基础地址和完整 `/chat/completions` 地址。
- 新增 AI 助手：支持 OpenAI、DeepSeek、硅基流动、FreeLLMAPI 预设和自定义 OpenAI 兼容接口。
- 工具栏新增 AI 按钮，命令面板新增 `Ctrl/Cmd+Shift+A` 快捷键；默认询问当前页面。
- 节点右键可仅将该节点及全部子节点转换为 Markdown，页面空白处右键恢复整页范围。
- 发送前显示节点数、字符数和 UTF-8 大小，超过可配置上限时禁止请求且不静默截断。
- AI 窗口新增转换、上传、模型处理和结果接收轨迹，并支持 Markdown 回答渲染与复制。
- 自定义 Header 拒绝非法名称、嵌套值和换行注入；API 密钥仅在用户主动发送时用于所选接口。
- 大纲模式仅在当前会话同步，不会成为下次启动模式。
- 导图、大纲、文章和通读共享语义阅读位置；关闭、重开、切换模式和跨子导图导航时恢复同一节点。
- 目标节点或子导图不存在时，按节点祖先和父导图挂载链逐级回退。
- 文件改名会迁移文章族键、目标路径和父级回退路径。

### 代码清理

- 删除 Markdown 后缀脑图转换、早期设置目录搬运、单图床设置转换和重复显示配置。
- 文章编号只保留 `articleNumberingMode` / `articleNumberingLevel`；剪贴板只写入当前多节点载荷。
- 删除未挂载的节点搜索弹窗、无调用方导出、包装函数、未使用字段、导入、局部变量和样式。
- 内容同步函数统一为 `syncNodeContentFields()`；通读进度只持久化语义位置，不再保存第二套百分比。
- 示例文件改用可读的 UTF-8 路径，并删除编码名称的重复副本。
- 删除一次性迁移与 CI 故障文档，重新生成当前函数参考。

### 工程与安全

- 文件名和图床输入处理集中到纯工具层，校验 URL、Header、multipart 和响应地址。
- 子导图创建与提取复用统一的文档初始化、导航和写入流程。
- TypeScript 开启 `noUnusedLocals` 和 `noUnusedParameters`，移除自定义 `typeRoots`。
- 独立测试扩展到 35 项，新增未引用 CSS 和示例路径清洁度检查。
- README、数据模型、架构、测试、维护和 Git 文档均更新为当前实现。

## 1.11.7

- 文章和通读模式新增右下角悬浮“回到顶部”按钮：默认隐藏，仅按阅读进度百分比显示；可通过 `0–100%` 滑块或直接输入阈值。

## 1.11.6

- 新增 `Ctrl/Cmd+A` 全选导图节点快捷键：会选中所有非主节点（包括折叠分支内的节点），主节点不会被选中或参与批量操作。

## 1.11.3

- 修复 Markdown 导入中的行内加粗：`**文字**` 会转换为插件的富文本加粗样式，节点文字不再显示星号，并在导图、大纲和文章视图中保持一致。

## 1.11.1

- 修复仅使用整行加粗文本编排的 Markdown 导入：首个主题不再吞并后续主题，普通文本和“好标题”等二级标签会保留在当前主题下。
- 修复带目录表格的 Markdown 导入：文件名会保留为根节点，一级主题保持同级，引用和正文段落会归属当前标题。

## 1.10.7

- 粘贴节点分支后默认收起粘贴出的节点及其后代，避免大分支立即展开。
- 导入 XMind、Markdown 或 JSON 后保持根节点展开，并默认收起全部非根分支。

## 1.10.6

- XMind、Markdown 和 JSON 文件导入现在会在导入窗口显示读取、解析与生成导图的阶段进度。
- XMind 与 Markdown 分别显示对应的解析状态；导入完成前保留 100% 进度，失败时显示具体原因。

## 1.10.5

- 修复 XMind 导入只保留首个画布或第一层主题的问题；现在保留嵌套主题、画布链接和未链接画布。
- 修复 Markdown 导入时标题下的列表被拉平到根节点的问题，保留标题和列表混用时的完整层级。

## 1.10.4

- 修复多选节点复制或剪切后，粘贴只保留最后一个节点的问题。
- 多选剪贴板现在按导图原始顺序保存全部顶层分支，粘贴后保留每个分支的完整后代内容并生成新的节点 ID。
- 保持旧版单节点剪贴板 JSON、Markdown、缩进文本和 HTML 列表的粘贴兼容性。

## 1.6.0

- 导图模式支持同级节点通过拖拽任意向上或向下排序。
- 拖到目标节点上方时插入目标之前，拖到下方时插入目标之后，拖到中间时仍作为目标子节点。
- 根节点只接受子节点拖入，禁止产生根节点同级。
- 禁止把节点拖入自身或自身后代，保留原有循环保护。
- 新增上方、下方插入指示线和中间子节点高亮。
- 同级排序与改变父子关系均进入统一撤销、重做、自动保存和三模式同步流程。
- 新增节点向上、向下、变更父级、循环保护和根节点保护测试。
- `.mindmap` 数据结构未改变，旧文件无需迁移。

## 1.5.1

- 为全部 11 个 TypeScript 源码模块添加模块级职责和边界说明。
- 为 376 个命名声明补齐 JSDoc，覆盖类型、接口、类、构造函数、顶层函数和类方法。
- 函数注释说明用途、参数、返回值、状态修改、兼容行为和关键失败路径。
- 对文档解析、模型规范化、富文本、布局、SVG、文章编号、全局搜索、子导图、图床上传、图片镜像、自动保存和撤销链路增加重点说明。
- 新增 `docs/ARCHITECTURE.md`，说明模块分层、加载保存、编辑事务和跨文件服务。
- 新增 `docs/DATA_MODEL.md`，完整说明 `.mindmap` 文档、节点、内容块、富文本、样式、子导图和图片镜像字段。
- 新增 `docs/SPECIAL_FEATURES.md`，详细介绍三模式、只读、文章目录、图床容灾、搜索索引、节点尺寸、渐细连线等特殊功能。
- 新增自动生成的 `docs/FUNCTION_REFERENCE.md`，列出全部函数、方法、类和接口的签名、位置与说明。
- 新增 `docs/DEVELOPMENT.md`，说明注释规范、测试重点、文档生成和发布流程。
- 新增 `npm run docs:generate` 与 `npm run docs:check`；`npm test` 会强制检查源码注释覆盖。
- 本版本不修改 `.mindmap` 数据结构，现有文件无需迁移。

## 1.5.0

- 子导图节点改为整块可点击：点击节点文字或周围空白区域都会直接进入对应子导图，不再要求精确点中文字。
- 子导图节点增加完整悬停和键盘焦点反馈；右键菜单仍可编辑、调整样式和执行其他节点操作。
- 新增全局节点文字对齐设置：左对齐、居中、右对齐。
- 节点编辑窗口新增单节点文字对齐覆盖，可选择跟随全局或独立设置。
- 新增节点宽度和最小高度设置，宽度范围 100–900 像素，高度范围 36–600 像素。
- 选中节点后显示右下角拖动控制点，可直接调整节点大小；双击控制点或右键“恢复节点自动大小”可还原。
- 节点文字根据实际宽度自动换行；加宽后可保持单行，缩窄后自动形成多行。
- 布局计算、连线位置和 SVG 导出同步使用自定义节点尺寸、文字对齐和换行结果。
- 新增尺寸和对齐字段均为可选字段，现有 `.mindmap` 文件无需迁移。

## 1.4.7

- 优化导图模式中子导图标题的鼠标悬停效果。
- 修复子导图标题继承文字块 `width: 100%` 后，悬停背景铺满整行形成灰色横条的问题。
- 子导图链接现在只包裹标题文字本身，不改变节点原有背景、圆角和配色。
- 悬停反馈改为短下划线展开和右上箭头轻微移动；键盘聚焦仍保留清晰但紧凑的可访问性轮廓。

## 1.4.6

- 合并导图模式中的子导图节点标题与进入入口。
- 删除节点下方重复显示的同名“进入子导图”卡片。
- 子导图节点的第一段文字现在直接作为链接，标题末尾显示轻量箭头。
- 点击标题进入子导图；点击节点其他区域仍用于选中，编辑可继续使用双击节点空白区域、F2 或右键菜单。
- 纯图片或无文字的子导图节点使用右上角小型箭头入口，不再生成同名文字。
- 节点高度不再为重复子导图卡片额外预留空间，导图更紧凑。
- SVG 导出以标题末尾箭头表示子导图，不再绘制重复卡片。

## 1.4.5

- 修复节点编辑器中“文章模式不自动编号”复选框被通用输入框样式拉成长条的问题。
- 复选框恢复为固定 16×16 像素的小方框，与说明文字横向居中对齐。
- 该选项独占一整行，并增加轻量边框和背景，窄窗口下也不会变形。

# Changelog

## Unreleased

- Fixed global-search result navigation so the search panel is synchronously hidden and detached before navigation, preventing an empty modal shell from remaining visible; duplicate-click protection is retained.
- Added regression coverage for search-result modal closing and Windows file-URL path resolution in the test suite.

- 优化“收起所有节点”后的布局过渡：保留当前视口并平滑移动保留节点，不再跳转到固定位置。
- 修复插件更新后使用浏览器级重载导致编辑器事件状态残留的问题；更新完成后改为提示完整重启 Obsidian。
- 修复末端正文转序号后，富文本异步重渲染会移除序号而又同时隐藏原标识的问题。
- Fix terminal body conversion to retain its generated next-level sequence prefix and remove the obsolete marker alignment gutter in article and continuous-reading modes.
- 可编辑导图中选中节点后按 `Space` 现在会进入快速内联编辑；节点展开/收起仍可使用工具栏和右键菜单。
- 修复按 `Enter` 新建同级节点后，快速输入框会因创建按键的残留 `keyup` 事件立即失去焦点的问题。
- 修复折叠或展开代码后导图仍按展开高度保留大片空白的问题；富内容节点现在按实际尺寸重新计算分支位置并平滑过渡。
- Markdown 导入新增 Obsidian 图片嵌入语法支持；`![[图片.png]]` 会转换为图片内容块，不再作为普通文字保留。
- 快捷键配置新增全局搜索，并统一支持点击后直接录制按键组合；默认保存文件夹移动至“文件与资源”，并与仅存放图片、截图和子导图资源的资源文件夹明确区分。清空非唯一文字块并退出编辑时会自动移除该文字块。
- 代码语言下拉新增 INI。
- 全局搜索结果的“替换此节点”操作移至结果标题栏右侧并垂直居中；节点右键菜单新增“插入文字”，可在当前节点末尾追加文字块并立即快速编辑。
- 修复导图编辑器拦截 `Ctrl/Cmd+Shift+F` 导致全局搜索快捷键失效的问题；当前导图族搜索调整为 `Ctrl/Cmd+Alt+F`。
- 修复 Markdown 导入在未解析出子节点时凭空添加“主题 1”的问题；导图代码块改为单击选中、双击编辑。
- 图片预览直接覆盖 Obsidian 模态默认尺寸，接近全屏显示；文章代码行内编辑按原代码行数展开，并保留原本显示的行号栏。
- 代码语言下拉新增 Dockerfile；工具栏和节点菜单的“插入代码”会追加代码块；代码块、表格块右键菜单改为只编辑或移除鼠标所在的当前块。
- 修复文章页面从阅读状态切换到编辑状态后，代码块因未重新绑定双击事件而无法直接行内编辑的问题。
- 修复文章或通读中调整图片对齐、尺寸等内容后，完整重绘会把阅读位置跳回文章开头的问题。
- 图片预览窗口扩大至接近全屏的 98% 宽、94% 高，并保持无滚动条的 16:9 预览舞台。
- 导入文件会记住上一次选择的本地文件夹；Obsidian 桌面端下次打开原生选择器时直接进入该目录，取消选择不会再弹出备用选择器。
- 题库改为配置文件夹内导图专用的整页模式：支持单选、多选、大题连续作答，按标准答案自动判题，错题自动收录，并在提交后进入下一题。
- 题库练习新增查看参考答案与解析步骤，保留当前判题结果后再进入下一题；题库文件夹设置归入“文件夹”分类。
- 修复粘贴为子节点后首次双击快速编辑重复创建文字块的问题。
- 点击节点后粘贴普通文字时，统一作为该节点的子节点插入。
- 代码块语言改为默认 Bash 的常用语言下拉框，并支持手动输入自定义语言。
- 表格和代码现在也属于可排序内容块，可在“编辑节点内容”中上下调整位置。

## 1.4.4

- Replaced the parent-map control attached to the root node with a fixed breadcrumb overlay in the upper-left corner of mind-map mode.
- Added a conventional back-arrow button plus “parent › current” breadcrumb text.
- The navigation stays fixed while the canvas pans or zooms and no longer changes the root node's appearance.
- Added responsive truncation, keyboard focus styling, translucent theme-aware background, and mobile sizing.

## 1.4.3
- Replace the abrupt top-right circular parent-return marker with an attached breadcrumb tab on the upper-left edge of the submap root node.
- The tab shows a left arrow and the parent-map title, uses the current root-node theme colors, and truncates long titles without covering the node content.
- In mind-map mode, parent navigation is handled by the root-node tab; outline and article modes continue to use the full-width parent breadcrumb bar.
- The return control stops node selection and drag interactions, supports keyboard focus, and opens the recorded parent node directly.

## 1.4.2
- Remove the unconventional top-right round parent-back button from submap root nodes. Parent navigation now relies on the dedicated top breadcrumb/navigation bar.
- Existing submaps that still contain an auto-generated parent wiki-link on the root node will no longer render that floating dot button.
- Newly created submaps no longer write the parent file link into the root node.

# 更新记录

## 1.38.7

- 优化大文章缓存命中路径：预扫描只判断指纹并记录可复用条目，不再在首屏显示前同步对所有章节执行 `innerHTML` 恢复、净化和交互绑定；缓存章节与普通章节现在共用渐进帧预算，并优先恢复当前节点关系链。
- 为单次文章渲染增加节点内容块 `WeakMap`，文章标题解析、缓存资格判断、正文渲染和 hydration 复用同一份规范化结果，避免包含表格、图片或富文本的节点被重复转换。
- 缓存节点 hydration 对全部 `[data-block-id]` 一次建索引，再在同 ID 小集合中定位段落、图片和表格；多内容块章节由重复全 DOM 查询收敛为一次线性扫描。
- `buildArticleNodeInfo()` 支持注入主文字读取回调，并合并同级标题/末端正文统计遍历，使文章模式可复用内容块缓存而不改变通读等现有调用行为。
- 新增缓存恢复必须位于帧批次、内容块单次规范化和块元素索引专项测试；统一版本元数据为 1.38.7。

## 1.4.1

- 修复文章模式中独立子导图节点没有生成编号的问题。
- 子导图节点现在即使没有本地子节点，只要存在 `submap.path` 也会被识别为文章标题。
- 文章编号跨父子导图连续计算：父导图的“第一章”进入子导图后继续为“第一节”，不会重新从章开始。
- 显示模式改为全局记忆；切换模式后，之后打开的所有父导图和子导图保持同一模式。
- 每个 `.mindmap` 文件中旧的 `view.mode` 仅作兼容读取，不再控制打开模式；只读状态仍按文件保存。
- 大纲和文章模式中的子导图入口改为节点标题文字超链接，移除独立“进入子导图”按钮。
- 顶层文章模式新增跨子导图的独立目录页。
- 目录递归收录后代子导图的章节标题，点击可打开对应文件并定位节点。
- 现有父子导图无需重新创建，数据结构版本保持 10。

## 1.4.0

- 新增导图、大纲、文章三种同步显示模式。
- 三种模式共用同一份节点数据，在任意模式修改都会同步到其他模式。
- 工具栏新增模式切换按钮，并可在设置中选择显示哪些模式。
- 新增只读锁，三种模式均可在只读和编辑状态之间切换。
- 只读模式禁用新增、删除、拖拽、粘贴、样式和内容修改，同时保留查看、搜索、链接、缩放及临时展开功能。
- 大纲模式支持层级缩进、直接编辑文字、任务状态、备注和子导图入口。
- 文章模式新增自动目录与层级编号：章、节、一、（一）、1.、（1）。
- 仅有子级的节点参与标题编号；末端节点作为正文，不继续编号也不占用序号。
- 节点新增“文章模式不自动编号”，适合前言、注释、摘要和附录。
- 当前模式与只读状态保存到 `.mindmap` 文件，数据格式升级为版本 10。
- 设置页新增“一键还原所有插件设置”，不会修改脑图原文件。

## 1.3.1

- 修复在父导图中使用 `Ctrl/Cmd + F` 无法搜索独立子导图节点的问题。
- 当前搜索改为递归搜索父导图及其全部后代子导图。
- 搜索打开时按需刷新当前导图族，不需要重新创建子导图，也不需要手动重建整个索引。
- 支持根据父节点的 `submap.path` 和子导图的 `navigation.parentPath` 双向恢复父子关系。
- 从子导图发起搜索时，会自动向上找到最高层父导图，再搜索完整导图树。
- 搜索结果增加完整父子导图层级，例如“古诗 › 唐诗 › 李白”。
- 支持使用多个层级关键词搜索，例如“古诗 唐诗 李白”。
- 搜索索引格式升级为版本 2；旧的平铺索引会自动重建，不影响 `.mindmap` 原文件。
- `Ctrl/Cmd + Shift + F` 继续用于整个仓库的全局搜索。

## 1.3.0

- 新增跨整个仓库的思维导图全局搜索。
- 全量索引所有 `.mindmap` 文件，包括普通子节点、折叠分支和独立子导图。
- 索引节点文字、富文本、备注、标签、链接、表格、代码、图片说明和子导图路径。
- 搜索结果可直接打开目标导图，展开祖先分支并定位到具体节点。
- 新增本地增量索引库 `mindmap-search-index.json`，避免每次搜索重新扫描全部文件。
- 监听脑图的新建、修改、重命名和删除并自动维护索引。
- 新增工具栏按钮、侧边栏按钮、命令面板命令和 `Ctrl/Cmd + Shift + F` 快捷键。
- 设置页新增索引统计、结果数量上限和手动重建按钮。
- 改进跨文件打开后的节点定位，视图尚未完成加载时也会保留待定位节点。

## 1.2.1

- 修复“从粗到细”连线模式在两三层导图中变化不明显的问题。
- 连线宽度改为按当前可见导图的实际最大层级插值：一级连线使用起始粗细，最深层连线达到末端最细宽度。
- 同时写入 SVG `stroke-width` 属性和内联样式，避免部分 Obsidian 社区主题把所有连线覆盖为统一宽度。
- SVG 导出与编辑画布使用相同的实际层级渐细算法。

# Changelog

## 1.2.0

- 新增 10 套可预览、可一键应用的脑图主题模板。
- 主题同时控制背景、中心主题、节点、文字、分支颜色、字体和连线。
- 新增彩色分支：一级分支及其全部后代使用一致的连线和节点边框颜色。
- 新增中心主题背景色与文字颜色设置。
- 连线新增“统一粗细”和“从粗到细”两种模式。
- 渐细连线可设置起始粗细和末端最细宽度。
- SVG 导出和静态预览保留主题、分支颜色及层级线宽。
- 主题与外观窗口新增主题预览卡片；插件设置页新增默认主题选择。
- 数据格式升级为版本 9，旧文件可直接读取。
- 旧用户的既有全局配置默认保持统一粗细且不自动开启彩色分支，避免升级后外观突变。

## 1.1.0

- 新增多图床镜像自动故障转移。
- 当前图片地址加载报错或超过设定时间后，按镜像顺序尝试下一个地址。
- 备用地址成功加载后，自动替换为新的主地址并保存脑图。
- 当前镜像失效后，下一次会优先从当前地址之后的镜像继续尝试，再回到更早的镜像，避免重复地址。
- 可选择将本地图片作为所有远程镜像失败后的最后回退。
- 新增 2–30 秒单镜像等待时间设置。
- 镜像记录新增最近成功时间、最近失败时间和失败次数，可向后兼容旧文件。
- 数据格式版本仍为 8，旧 `.mindmap` 文件无需迁移。

## 1.0.0

- 支持配置多个图床。
- 支持粘贴图片后延迟自动上传。
- 支持手动选择一个或多个图床。
- 所有目标图床成功后可安全删除本地图片。
- 新增图床 API 实际上传连通性检测。
