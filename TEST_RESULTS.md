# Test Results

版本：1.39.0

- 单元测试：275 / 275 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1055 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 文件浏览器 MutationObserver 只收集新增 DOM 子树和 `data-path` 变化目标，并在同一批次中去除嵌套扫描根；文章渲染、弹窗和其他无关 DOM 变化不会进入筛选队列。
- 仓库 `create`、`modify`、`delete`、`rename` 回调不再主动安排文件树全量扫描；连续编辑或自动保存 `.mindmap` 不会重复遍历全部 `[data-path]` 条目。
- 布局初始化和筛选设置规范化语义变化仍会完整刷新文件浏览器，局部新增或改路径条目复用同一份预编译筛选谓词。
- 文章缓存无待写变化时 `flush()` 不访问磁盘；访问旧缓存条目后会持久化新的 LRU 顺序，重复访问当前最新条目不会再次写盘。
- 文章缓存活动写入期间到达的连续更新会合并为一份后续稳定 JSON 快照，任意时刻最多只有一个缓存写入运行，最终磁盘内容为最新版本。
- 1.38.9 的设置保存合并、规则预编译和语义签名，以及 1.38.8 的设置分类与文章锁状态记忆继续通过完整回归。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、`.tmp/`、嵌套 ZIP 和本轮未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.39.0-handoff-987877.zip`。
- Codex ZIP 的中文目录和文件名使用标准 UTF-8 主文件名和 general-purpose bit 11；Linux Info-ZIP 实际解压后不得存在字面量 `#Uxxxx`。
- 安装 ZIP SHA-256：`04ef966a45b52f9b0ed3841c09686148fcf626a2898f169c053128049fb22d5a`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在 Win10 中打开大型文件浏览器目录，连续编辑并自动保存 `.mindmap`，确认文件树不闪烁、不全量重排，隐藏规则仍保持正确。
- 展开大型目录、新建和重命名不同后缀文件，确认只有新增或改路径条目更新隐藏状态；修改筛选设置时全部现有条目立即刷新。
- 依次打开多个已缓存文章并完全退出 Obsidian，重启后确认最近访问文件优先保留；快速编辑文章后立即退出，确认最新缓存快照可恢复且缓存 JSON 不损坏。
- 回归快速设置写入、设置分类展开记忆、文章“记住上次状态”以及大型文章渐进加载期间的滚动控制。
