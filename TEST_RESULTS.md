# Test Results

版本：1.39.12

- 单元测试：280 / 280 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1032 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 文章上下文未准备完成时不调用真实文章渲染器，只显示与最终落地一致的目录或正文骨架；“显示目录”不会先闪出原始正文。
- 目录跳章节和章节回目录共用对称过渡；返回目录取消正文阅读恢复并在真实目录挂载后停在顶部。
- 跨文件章节目标在 `setViewState()` 前进入短生命周期队列，新视图创建编辑器前同步消费目标并建立显式位置。
- 首个真实文章窗口直接使用目标节的节点 ID、层级和编号，不先生成父章节编号与子文件根标题的中间状态。
- 当前文件目录聚焦、5 KB 目标窗口、独占导航事务、布局稳定、边缘扩展、父级返回、编辑和导出专项保持通过。

交付包验证：

- 源码 ZIP 第一层固定为 `obsidian-mindmap-studio/`，安装 ZIP 固定为 `mindmap-studio/`，Codex ZIP 固定为 `Codex/`。
- 三份 ZIP 的非 ASCII 路径使用 UTF-8 主文件名与 general-purpose bit 11，并执行实际解压验证。
- 安装包 SHA-256：`440e3c99dd528cc034502babe68e0b4e8bd8c4b2ff999961529690abc5eafa9e`，已写入 `update.json`。
- 本轮交付后缀：`719588`。

仍需真实 Obsidian 手工验证：

- 在 Win10 Obsidian 中把顶层文章落地设置为“显示目录”，连续关闭和打开文件，确认首帧只出现目录骨架并直接进入目录，不闪出原始正文。
- 从目录进入章节，再从章节返回目录，确认两向都有轻量过渡，目录最终停在顶部。
- 点击“第三章 自然 → 第三节 天体运行”一类跨文件深层目录项，确认首个真实标题就是“第三节 天体运行”，不先出现“第三章 天体运行”。
- 快速连续点击当前文件与跨文件章节，确认只保留最后目标；等待图片、表格、代码和字体加载后也不发生二次滚动。
- 连续双向滚动跨越多个 5 KB 窗口，确认边缘流光不遮挡正文，向上补载不改变当前段落屏幕位置。
