# Test Results

版本：1.39.1

- 单元测试：278 / 278 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1056 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 文章编号只定义 1–8 级；第 9 级及更深标题继续保留真实结构深度，但 `articleNumberLabel()` 返回空标签，不再循环复用第 7 级的 `A.` 或第 8 级的 `（A）`。
- 复现用户截图对应的 `E. → （A）/（B） → 深层标题` 结构，确认 `（B）` 的子标题不会生成新的 `A. / B.` 前缀，显示标题只保留节点原文。
- 第 7、8 级同级编号超过 26 项时按 A–Z、AA、AB 继续递增，不再从 A 重复，避免同一父节点下出现重复序号。
- 正文、目录、通读和导出仍共用 `buildArticleNodeInfo()` 与 `articleNumberLabel()` 的单一解析结果，手动 1–8 级配置和既有编号样式保持兼容。
- 1.39.0 的文件浏览器增量筛选与文章缓存写入合并、1.38.9 的设置保存合并及 1.38.8 的状态记忆继续通过完整回归。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、`.tmp/`、嵌套 ZIP 和本轮未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.39.1-handoff-534522.zip`。
- Codex ZIP 的中文目录和文件名使用标准 UTF-8 主文件名和 general-purpose bit 11；Linux Info-ZIP 实际解压后不得存在字面量 `#Uxxxx`。
- 安装 ZIP SHA-256：`cda34b20ee4bf33f1f5bb325604f93656981c0e7b04638548c5c593b80e9e31d`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在 Win10 中打开截图对应的原始导图，确认 `E. → （B）` 下的“关键词区分、维度区分”等第 9 级标题不再出现自动 `A. / B.`，其层级缩进、字号、目录位置和折叠行为保持正确。
- 在文章、通读、HTML/Markdown/Word 导出中分别检查同一深层结构，确认编号边界一致且不会把生成序号写回节点原文。
- 建立 27 个以上第 7 或第 8 级同级标题，确认第 27 项显示 AA /（AA），并验证目录与导出一致。
- 回归大型文件浏览器局部筛选、文章缓存跨重启、设置快速写入、设置分类展开记忆和文章锁状态记忆。
