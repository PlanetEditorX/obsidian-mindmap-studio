# Test Results

版本：1.39.2

- 单元测试：282 / 282 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1061 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- `circledNumberLabel()` 在 1、20、21、35、36、50 的 Unicode 分段边界分别生成正确的 `①、⑳、㉑、㉟、㊱、㊿`；第 51、67 项返回普通十进制索引，交由展示层绘制圆圈。
- 67 个同级末端正文达到阈值后按 1–67 连续编号：前 50 项使用 Unicode 原生字符，第 51 与第 67 项进入 CSS 圆圈回退，不截断、不从 1 重新开始。
- 带圈末端序号独立于文章标题的八级编号边界；第 8 级标题下面的第 9 级末端正文仍可显示带圈序号，而原“上级标题的下一级文章序号”继续遵守八级边界。
- 插件全局设置和当前页面 `articleStyle.leafNumberingStyle` 覆盖链路均通过规范化测试；非法旧值回退为 `next-level`。
- 文章正文、通读、缓存指纹和导出共用同一编号结果。HTML 对 51 以上输出 `.circled-number` 结构，Markdown 与 Word 使用 `◯51` / `◯67` 可读回退；生成编号不写回节点原文。
- 1.39.1 的深层标题编号边界、1.39.0 的文件浏览器增量筛选与文章缓存写入合并、1.38.9 的设置保存合并及 1.38.8 的状态记忆继续通过完整回归。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、`.tmp/`、嵌套 ZIP 和本轮未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.39.2-handoff-356917.zip`。
- Codex ZIP 的中文目录和文件名使用标准 UTF-8 主文件名和 general-purpose bit 11；Linux Info-ZIP 实际解压后不得存在字面量 `#Uxxxx`。
- 安装 ZIP SHA-256：`db0fd2c79fcb2419f1db05205eaf295e7d6b3956bae7ce64345ce54a224c2999`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在 Win10 中将“末端正文序号样式”切换为“带圈数字”，建立至少 67 个同级末端正文，确认 ①、⑳、㉑、㉟、㊱、㊿、第 51 项和第 67 项的字体、圆圈尺寸、基线与行高一致。
- 在第 8 级标题下建立多个第 9 级末端正文，确认带圈序号正常显示，同时普通深层标题不重新循环为 `A. / B.`。
- 在文章、通读、缓存重开、HTML、Markdown 和 Word 导出中检查第 50/51 项切换边界；确认 Markdown/Word 的 `◯51` 回退可读，HTML 圆圈不被主题样式覆盖。
- 回归页面级样式覆盖、设置保存、文章锁状态记忆、大型文件浏览器局部筛选和文章缓存跨重启。
