# Test Results

版本：1.42.5

- 单元测试：**306 / 306 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1081 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮实现

- 工具栏按钮同时受用户可见配置和实时可用性控制；不可执行时收缩隐藏，不再保留禁用占位。
- 初始过滤完成后才启用过渡，模式、只读、选择和撤销历史变化时平滑更新。
- 截图与截图识别固定相邻；画布与缩放仅在导图模式出现。
- JSON、SVG、HTML、Word、PDF 和 Markdown 合并为末尾的“导入与导出”；只读状态只移除导入控件。
- 旧工具栏导出 ID 自动迁移到统一入口。

## 自动回归

- 验证按钮的模式、只读、节点和历史可用性分支。
- 验证零宽收缩、首帧防闪和减少动态效果。
- 验证截图组相邻、导入导出置尾及旧配置迁移。
- 验证统一弹窗包含全部导入导出格式。

## 仍需真实 Obsidian 手工验证

- 快速切换导图、文章、大纲、通读和题库，确认工具栏没有全量闪现。
- 切换只读，选择根节点、叶节点和分支节点，并执行撤销/重做，确认按钮平滑收缩与恢复。
- 确认截图两个入口始终相邻，统一导入导出始终位于最后；只读时仍可导出。

## 交付包验证

- 安装包 SHA-256：`a0a01ab281891615586c6358961b1ef5d2a41c55bd1d216c71cc94871415d25d`，已写入 `update.json`。
- 本轮交付后缀：`528008`。
