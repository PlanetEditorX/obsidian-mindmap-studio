# Test Results

版本：1.38.6

- 单元测试：264 / 264 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（52 个源码模块、1026 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 文章节点缓存指纹只序列化当前节点自身和层级、编号、只读状态、末端正文样式等渲染上下文，不再递归包含 `children` 后代子树。
- 后代节点内容变化不会使未变化祖先章节的缓存失效；当前节点内容、层级或编号上下文变化仍会生成不同指纹。
- 文档缓存摘要直接聚合已经计算的节点指纹，不再额外稳定序列化整份 `MindMapDocument`。
- 600 层链式节点的本地专项基准中，旧递归指纹路径约 331.4 ms，节点局部指纹约 1.47 ms；该数据用于确认复杂度优化，不作为跨设备固定性能承诺。
- 启动预载会先选择最近使用的快照，再按 oldest-first 顺序重建内存 Map；加入第 25 个缓存时正确淘汰最久未访问条目。
- Windows 与 Unix 文件分隔符在缓存兼容检查中统一规范化；磁盘节点表使用无原型对象，避免异常键影响缓存结构。
- 缓存修订提升为 `article-node-cache-v2`，旧快照会安全失效并在首次完整渲染后重建。
- Windows ZIP 中 `#Uxxxx` 兼容主文件名与 `0x7075` Unicode Path Extra Field 已在 Linux 中恢复为可读中文 examples 路径；最终源码 ZIP 使用标准 UTF-8 主文件名和 bit 11。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、运行时缓存、临时目录和嵌套 ZIP，并保留规范中文 examples 路径。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.38.6-handoff-563732.zip`。
- 安装 ZIP SHA-256：`4ec6137944253d77521803a37363de0e94123969d6470e1be04d40245d6e761c`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在 Win10 中第一次打开原先约 80 KB 的深层文章，确认首批内容仍快速出现，后台补齐期间滚动位置不回顶部。
- 关闭并重新打开同一文章，确认节点级缓存能直接恢复大部分正文；完全退出并重启 Obsidian 后再次确认磁盘缓存命中。
- 只修改深层末端节点后重新打开，确认其祖先章节无需重建，修改节点显示新内容且图片、表格、题目和行内编辑交互正常。
- 调整章节层级、编号、文章字体或末端正文样式，确认受影响节点正确失效并重建。
- 打开含代码块的文章，确认代码块节点正常重新渲染，其他缓存节点仍可复用。
- 在 Win10 与 Linux 分别解压源码 ZIP，确认 `examples/中国文学示例.mindmap`、`examples/古诗.mindmap` 和 `examples/MindMap Assets/古诗/唐诗.mindmap` 均保持可读中文名。
