# Test Results

版本：1.38.5

- 单元测试：263 / 263 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（52 个源码模块、1025 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 新增插件级文章渲染缓存，插件加载阶段从私有 JSON 文件预载最近使用的缓存，`TextFileView` 打开文件时可同步读取，不需要先等待一次磁盘 I/O。
- 缓存以文件路径、文档指纹、文章展示签名和节点指纹判定有效性；文档及展示配置完全未变化时，可直接恢复全部可缓存节点的静态正文。
- 局部修改后只重新渲染指纹变化的节点；层级、章节编号或文章样式改变时，受影响节点会因指纹变化自动失效，其他节点继续复用。
- 缓存恢复后按帧重新绑定当前编辑器实例的文字编辑、子图链接、图片、表格列宽和题目交互，避免复用旧事件监听器或旧视图引用。
- 含异步 Markdown 代码组件的节点不写入持久缓存，重新打开时按正常路径安全渲染；其余节点仍可直接复用。
- 持久化内容在恢复前拒绝 `script`、`iframe`、`object`、`embed` 等危险元素，并移除内联事件属性。
- 缓存采用 24 个文件、约 1200 万字符总容量及单节点约 100 万字符上限的 LRU 策略；写盘采用 800 ms 防抖和串行提交。
- 文件删除时同步清理缓存；文件重命名时迁移缓存键；从 `.mindmap` 改为其他扩展名时删除旧缓存。
- 缓存架构版本、渲染器版本或结构校验不通过时自动忽略并回退到现有渐进渲染，不影响文件打开。
- 新增稳定指纹、路径规范化、启动预载、同步命中、磁盘持久化、代码块排除及文章渲染缓存契约测试。
- 1.38.3 的按需导图布局与首批正文显示、1.38.4 的渐进加载滚动位置保护继续通过全部回归。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、运行时缓存、临时目录和嵌套 ZIP。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.38.5-handoff-<六位后缀>.zip`。
- 安装 ZIP SHA-256：`49fdb2ecc75a626e0795ca7034afd4fd491c7b8b336bbb4c908faa3253ee104f`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在 Win10 中第一次打开原先约 80 KB 的文章并等待后台完成，关闭后重新打开，确认第二次能直接显示接近完整正文，而不是重新持续补载半分钟。
- 完全退出并重启 Obsidian 后再次打开同一文件，确认磁盘持久缓存仍可命中。
- 只修改一个普通正文节点后关闭并重新打开，确认未变化章节直接恢复，修改节点显示新内容且交互正常。
- 修改章节层级、编号配置和文章字体/间距设置，确认受影响节点自动失效并正确重建。
- 测试图片预览、图片右键、表格双击、表格列宽拖动、题目展开和正文双击编辑，确认缓存恢复后的事件均绑定到当前视图。
- 打开含代码块的文章，确认代码块节点重新渲染且其他节点仍可缓存复用。
