# Test Results

版本：1.35.3

- 单元测试：212 / 212 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（50 个源码模块、959 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 截图与截图并识别继续使用独立命令、快捷键、工具栏和节点右键入口。
- 截图源改为本机非交互式整屏抓取；本机命令失败时仅尝试渲染器可用的 Electron `desktopCapturer`。
- 截图编辑器通过独立覆盖窗口启动，窗口创建受限时使用 Obsidian 内嵌全屏覆盖层。
- 已删除系统交互式区域截图的静默回退，抓取或覆盖层创建失败时返回明确错误。
- 选区使用 3px 高亮蓝色边框、白色内描边、外阴影和八方向高对比手柄；左上角显示全局位置与尺寸。
- 底部工具栏包含几何图形、画笔、箭头、文字、序号、马赛克、橡皮擦、识别并复制、固定、下载、取消和复制。
- 普通截图在鼠标释放后保持打开，只在双击选区、点击复制或按 Enter 后完成。
- 截图并识别在完成拖拽或边框调整后等待 3 秒；鼠标位于选区内、移动选区、调整边框或使用工具栏时会重置等待时间。
- Windows 无法取得 Electron 窗口句柄且启用“截图时隐藏 Obsidian”时，由 PowerShell 抓屏脚本临时最小化并恢复前台窗口。
- 桌面 Node/Electron API 继续动态加载，移动端插件加载契约保持通过。
- 源码包缺失的三个 UTF-8 中文示例路径已经恢复并通过仓库检查。

仍需真实 Obsidian 桌面端手工冒烟：

- Windows 中独立覆盖窗口是否按目标显示器全屏打开，以及 Obsidian 内嵌覆盖层回退是否可见。
- macOS 屏幕录制权限、Linux 可用抓屏命令，以及负坐标多显示器布局。
- 100%、125%、150%、200% DPI 下选区、标注和导出像素对应关系。
- 固定窗口的置顶、拖动和缩放；真实 AI 视觉接口与本地 Tesseract OCR。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、临时目录和嵌套 ZIP。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`。
- 中文示例和中文文档路径使用 ZIP UTF-8 文件名标志，并执行实际解压验证。
- 安装 ZIP SHA-256：`1ed6d87b5c78691ff622758dd9e15471987f72e9347f63f00b512f2661f1ff16`，已写入 `update.json`。
