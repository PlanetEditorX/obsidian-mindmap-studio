# Test Results

版本：1.41.11

- 单元测试：**298 / 298 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1086 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮根因

- “主题与外观”按钮被注册为编辑专用控件，并且保存入口复用了会调用 `ensureEditable()` 的内容修改链路，所以文章或通读锁定后按钮和保存都会被拦截。
- 阅读纸张、字体、目录与末端正文设置仍保留在独立“文章样式”弹窗中，工具栏入口只在文章模式显示，通读虽使用同一数据却无法直接进入，造成作用范围与入口含义不一致。

## 本轮实现

- “主题与外观”改为所有模式可用，不进入只读禁用列表。
- 新增 `mutatePresentation()`，只读时仍可保存主题、阅读样式、文章编号、目录、缩略导航和代码外观，同时保留阅读位置。
- 独立“文章样式”弹窗与工具栏入口删除，全部控件合并为“主题与外观 → 阅读样式”，文章与通读共用。
- 旧工具栏 ID `article-style` 在设置加载时映射到 `appearance`。

## 自动回归

- 验证只读状态下外观按钮不是编辑专用控件，外观保存不调用 `ensureEditable()`。
- 验证阅读样式写入和恢复全局默认同时更新 `document.articleStyle`。
- 验证工具栏和设置列表不再出现独立文章样式入口。
- 验证插件全局“阅读样式”分组包含目录、缩略导航和末端正文默认项。
- 验证旧 `article-style` 可见项与顺序迁移为 `appearance`。

## 仍需真实 Obsidian 手工验证

- 在文章和通读模式分别锁定后打开“主题与外观”，修改阅读样式并应用，确认正文不可编辑但外观立即变化。
- 从文章切换到通读，确认预设、字体、纸张、目录样式和末端正文设置完全一致。
- 使用旧版 data.json（工具栏含 `article-style`）升级，确认只显示一个“主题与外观”入口。

## 交付包验证

- 安装包 SHA-256：`e925e5ed5788b2bac90c5cc130c65a79983f791f5d13c496db0114db92173e62`，已写入 `update.json`。
- 本轮交付后缀：`635383`。
