# Test Results

版本：1.41.3

- 单元测试：**290 / 290 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1077 个具名声明**
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

## 本轮根因

- 真实调试日志显示，第二次表格列宽拖动后，渲染文件从 `中国 3.mindmap` 无用户导航地切换为 `中国 2.mindmap`。
- 随后的阅读位置恢复仍指向旧文件，触发父级文件打开，最终表现为文章闪回目录或显示原始父文章。
- 根因由两条链路叠加：列宽保存同步重建文章 DOM，以及任何保存都尝试按中心标题同步文件名。

## 本轮实现

- 列宽拖动改为当前列和右侧相邻列守恒调整；一侧增宽时另一侧等量缩窄，总宽度保持不变。
- 保存列宽按总和转换为百分比，表格默认 100% 适配正文页面；长内容自动换行，外壳隐藏横向溢出。
- 最后一列不再创建无效的右侧拖动柄。
- 释放拖动后只记录历史、更新稳定表格块并触发保存，不调用文章整页重绘，也不安排延迟文章族上下文刷新。
- 仅当中心标题相对最近一次加载或保存确实改变时才同步文件名；列宽、样式和正文保存不再误改路径。

## 自动回归

- 相邻列算法覆盖左右极限和最小列宽边界。
- 表格列宽按百分比应用并始终为页面宽度；末列无拖动柄。
- 列宽提交不会调用通用 `mutate()` 或同步 `render()`，但仍保留历史与保存通知。
- CSS 契约验证固定布局、单元格长内容换行和无横向滚动。
- 文件名与中心标题原本不一致时，仅调整列宽不会重命名；真实修改中心标题仍保留原同步能力。

## 仍需真实 Obsidian 手工验证

- 在 Win10 Obsidian 文章模式连续拖动多个表头分隔线，确认当前章节、滚动位置和目录/正文落点不变。
- 在窄侧栏、不同缩放比例和长中英文内容下确认表格始终适配正文宽度且没有横向滚动条。
- 使用文件名与中心标题不一致的子导图调整列宽，确认文件路径不变；随后修改中心标题，确认预期重命名仍正常。
- 在通读模式检查同一张表格的比例列宽、换行和窗口尺寸变化。

## 交付包验证

- 安装包 SHA-256：`ce2e22339e7653486ad4f8efaf5141c1af25f86353fc6b7dc8f5adebf272a67d`，已写入 `update.json`。
- 本轮交付后缀：`640770`。
