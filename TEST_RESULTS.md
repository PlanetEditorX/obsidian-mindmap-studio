# Test Results

版本：1.35.3

- 单元测试：210 / 210 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（50 个源码模块、950 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- “截图与识别”位于“AI 助手”之后，作为独立一级设置分类。
- “截图”和“截图并识别”拥有独立命令、独立快捷键、独立顶部工具栏和节点右键入口。
- 旧“截图后自动识图”开关不再存在；普通截图、截图并识别、识别并复制保持三条独立动作边界。
- 截图覆盖层包含八方向调整手柄、全局位置与尺寸显示，以及几何图形、画笔、箭头、文字、序号、马赛克、橡皮擦、识别并复制、固定、下载、取消、复制。
- 节点右键截图在开始前冻结插入目标，截图期间焦点变化不会改变落点。
- Electron 与 Node.js API 继续动态获取；高级覆盖层不可用时仍可回退系统区域截图。

仍需真实 Obsidian 桌面端手工冒烟：

- Windows、macOS、Linux 的屏幕录制权限、系统截图回退和多显示器坐标。
- 高 DPI 缩放下选区、标注与导出像素的对应关系。
- 固定窗口的置顶、拖动与缩放体验。
- AI 视觉接口及本地 Tesseract OCR 的实际识别与剪贴板行为。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、`.tmp/`、`Codex/`、未修改的 `examples/` 和嵌套 ZIP。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，包含 `main.js`、`manifest.json`、`styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`。
- 中文文档路径使用 ZIP UTF-8 文件名标志，并通过实际解压验证。
- 安装 ZIP SHA-256：`2862071cc469396d7d3dbaa0df54a2aa088155bbe64406908bde3ed526be21c1`，已写入 `update.json`。

