# Test Results

版本：1.40.10

- 单元测试：**288 / 288 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**54 个源码模块、1052 个具名声明**
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

## 本轮实现

- “加载前文/加载后文”按钮改为 Flex 双轴居中，并固定 `line-height: 1.2`，避免 Obsidian 按钮主题的行高或内边距让文字视觉偏下。
- 实际加载状态新增圆角边框跑马灯：一小段较深的主题强调色通过 `conic-gradient` 沿按钮周边连续运行。
- 原有按钮内部流光保留；边框跑马灯只占用边框遮罩区域，不覆盖按钮文字或已渲染正文。
- 边框颜色使用 Obsidian 当前主题的强调色及悬停强调色，不写死亮色或暗色主题颜色。
- 系统启用“减少动态效果”时，边框跑马灯和内部流光都停止动画。

## 自动回归

- CSS 契约验证按钮使用 `display: flex`、`align-items: center` 和 `justify-content: center`。
- CSS 契约验证边框反馈使用已注册角度变量、`conic-gradient`、圆角遮罩和 1.35 秒线性动画。
- CSS 契约验证 `prefers-reduced-motion: reduce` 同时覆盖边框跑马灯和内部流光。
- 文章 5 KB 目标窗口、边缘扩展、章节导航和目录返回原有回归保持通过。

## 仍需真实 Obsidian 手工验证

- 在 Win10 Obsidian 1.12.7 中触发“加载前文”和“加载后文”，确认文字在不同主题、界面缩放和字体设置下视觉居中。
- 确认较深高亮段沿完整胶囊圆角连续运行，四个转角没有断裂、裁切或方角。
- 分别检查浅色与深色主题下的对比度，确保高亮明显但不过亮。
- 开启操作系统“减少动态效果”，确认边框和内部流光均停止，不影响按钮点击和分片加载。

## 交付包验证

- 安装包 SHA-256：`c0e123141851db252beb9cf3537a9dc0911934168f0e06ea89a092123c6256e8`，已写入 `update.json`。
- 本轮交付后缀：`347612`。
