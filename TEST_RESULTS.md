# Test Results

版本：1.39.11

- 单元测试：279 / 279 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1030 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 文章当前位置采集只扫描 `.mms-article-document-title[data-node-id]` 和 `.mms-article-node[data-node-id]`，不再把带根节点 ID 的 `.mms-article-page` 误判为当前章节。
- 章节边界使用下边界排除规则；锚点位于两个章节交界像素时不会同时归属上一章节，也不会因页面空白回退到根节点。
- 每次章节恢复建立递增令牌；后续点击会取消旧的 20 ms 重试、双动画帧、`ResizeObserver` 和最长等待定时器，确保最后一次导航独占 `scrollTop`。
- 异步文章上下文 `setOptions()` 重绘优先沿用仍活动的显式 `ReadingLocation`，不会从骨架替换或正在变化的 DOM 捕获旧位置。
- 目标到位后，文章页尺寸变化会在最长 5 秒内重新校准同一语义锚点，覆盖图片、表格、代码和字体晚到造成的高度变化。
- 滚轮、触摸、指针以及 PageUp、PageDown、Home、End、上下方向键或空格翻页会立即取消自动校准，把滚动控制权交还用户。
- 精确滚动仅在差值超过 0.5 px 时写入，避免布局观察期间因亚像素舍入产生抖动。
- 5 KB 目标窗口、入口骨架、边缘流光、向上高度补偿、父级返回、折叠、缩略导航、图片、表格、代码、编辑和导出专项保持通过。

交付包验证：

- 源码 ZIP 第一层固定为 `obsidian-mindmap-studio/`，安装 ZIP 固定为 `mindmap-studio/`，Codex ZIP 固定为 `Codex/`。
- 三份 ZIP 的非 ASCII 路径使用 UTF-8 主文件名与 general-purpose bit 11，并执行实际解压验证。
- 安装包 SHA-256：`e1b64e04b4e495649f7d861348969b1c1f85e6852441eb9055ae76e2a1243ff6`，已写入 `update.json`。
- 本轮交付后缀：`986549`。

仍需真实 Obsidian 手工验证：

- 在 Win10 Obsidian 中从外部目录依次点击当前文件内章节、跨文件深层章节和文档标题，确认每次都落到精确目标而不是页首。
- 快速连续点击三个距离很远的子章节，确认只停在最后一次点击；等待 5 秒也不会被旧定时器或旧动画帧拉到其他章节。
- 在目标章节上方放置延迟加载图片、宽表格和较长代码块，确认内容撑高后目标仍稳定；随后滚轮、拖动滚动条和 PageDown 能立即接管且不会被拉回。
- 连续双向滚动跨越多个 5 KB 窗口，确认边缘流光不遮挡正文，向上补载不改变当前段落屏幕位置。
