# Test Results

版本：1.37.0

- 单元测试：229 / 229 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（51 个源码模块、991 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 图片所有候选来源失败时，导图、文章、通读和大纲均显示原始/镜像/本地地址及“复制地址”按钮。
- 图片失败终态不再触发保存或整页刷新，避免失败重试造成保存循环。
- 外部图片粘贴在异步保存前冻结节点 ID 和文章内容块 ID；旧文章焦点不能覆盖导图/大纲当前选择，目标消失时不回退到其他节点。
- 多张到期图片按 `TFile` 批处理，同一文件串行上传，只显示成功/失败汇总通知。
- 上传网络结果只生成 `MindMapImageUploadPatch`，按节点与图片块 ID 合并到最新打开文档或重新读取的最新磁盘文档；上传期间的文字编辑和兄弟节点保持不变。
- 本地图片路径已经变化的过期上传任务会被忽略；自动上传批次不调用整文档 `refreshOpenMindMap()`。
- 文章上一篇、下一篇、返回上一级和阅读完成导航不再设置 `title` 或 `aria-label`，避免 Obsidian 生成悬浮提示。
- 中文示例路径已恢复为 `examples/古诗.mindmap`、`examples/中国文学示例.mindmap` 和 `examples/MindMap Assets/古诗/唐诗.mindmap`。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、Codex 和嵌套 ZIP。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.37.0-handoff-598763.zip`。
- 中文路径使用 ZIP UTF-8 文件名标志，并执行实际解压验证。
- 安装 ZIP SHA-256：`534d8da6a8eade3be2d318489a3e77e83ef33268055ebf07981b8c66531b07eb`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 打开包含多张本地待上传图片的导图，确认只出现一次汇总通知且界面不闪烁；上传期间继续编辑节点，关闭并重开后所有节点仍完整。
- 先在文章中编辑，再切回导图点击其他节点粘贴外部图片，确认图片只进入当前节点。
- 断开图片地址或移动本地文件，确认四种模式显示可复制地址。
