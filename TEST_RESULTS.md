# MindMap Studio 1.15.2 全书目录层级修复测试结果

## 问题与修复

- 原因：`ArticleTocEntry.depth` 同时被用于文章编号层级和目录显示层级。把最高编号层级设为 5 后，一级标题的 `depth` 变成 5；而“文章目录最大层级”默认是 3，因此通读模式把全部目录项过滤掉。
- 修复：保留 `depth` 表示编号层级，新增运行时字段 `tocDepth` 表示相对目录结构层级。目录过滤和缩进统一使用 `tocDepth`。
- 示例：`1.相得益彰` 的编号层级仍为 5，但它在全书目录中的结构层级为 1，所以目录最大层级设为 3 时仍会显示。
- 兼容：缺少 `tocDepth` 的旧运行时目录对象会回退到 `depth`；`.mindmap` 文件结构未改变，无需迁移。

## 验证结果

- `npm test` 通过：模型、编号、相对目录层级、布局、搜索、导入导出及源码回归测试全部通过。
- 文档覆盖检查通过：27 个 TypeScript 模块中的 503 个命名声明均具备 JSDoc。
- 已重新运行 `npm run docs:generate`，更新 `docs/FUNCTION_REFERENCE.md`。
- `main.js` 通过 `node --check` JavaScript 语法检查。
- `main.js` 使用 Obsidian API 运行时替身完成加载冒烟测试，默认插件导出为 `MindMapStudioPlugin`。
- 构建产物已核验包含：独立 `tocDepth`、目录相对层级过滤、相对层级缩进和旧对象兼容回退。

## 回归覆盖

- 最高编号层级为 5 时，一级标题显示 `1.`，但全书目录层级仍为 1。
- 目录最大层级为 3 时，上述一级目录项不会被过滤。
- 二级、三级目录按真实父子结构缩进，不按 `1.`、`（1）` 等编号样式推断缩进。
- 通读模式“全书目录”和文章模式“目录”共用相同的相对层级解析函数。
- 手动层级、自动编号、关闭编号及旧版目录对象保持兼容。

## 环境说明

上传的源码包未包含 `node_modules`，当前运行环境无法从依赖仓库恢复完整开发依赖。因此本次 `npm run build` 的 TypeScript 前置检查无法在该环境中复现，报错来自缺少 `obsidian` 类型包，而非本次修改代码。交付的 `main.js` 基于上一版已验证的 1.15.2 单文件生产包进行对应模块更新，并完成语法、加载与行为标记核验。临时测试依赖不会包含在源码交付包中。
