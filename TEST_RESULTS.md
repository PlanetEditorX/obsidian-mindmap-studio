# Test Results

版本：1.42.10

- 单元测试：**314 / 314 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**56 个源码模块、1098 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮根因

- 公式渲染按富文本运行段分别匹配美元分隔符，分隔符被颜色或加粗边界拆开后会显示原始源码。
- 任何 `$$...$$` 都被强制当作独立公式，即使同一个文字块前面还有“通项公式：”等说明。
- AI 或人工公式中直接使用“首项、项数、中间项”等中文标签时，MathJax 可能拒绝裸露中文并回退成原始美元源码。
- 公式编辑器允许用户粘贴已经带 `$` 的公式，再次保存时会重复包裹，生成双美元或不对称分隔符。

## 本轮实现

- 新增纯函数公式解析器，合并完整文字后识别公式并恢复历史重复美元。
- 双美元公式只有在整个文字块仅包含公式时才独立居中；和说明文字共存时按行内公式显示。
- 中文标签只在 MathJax 渲染副本中转换为 `\text{...}`，原始文档内容不改写。
- 公式编辑器预览和保存前清理外层美元分隔符，再按当前选择的模式统一插入。

## 自动回归

- 验证 `通项公式：$$a_n=...$$` 按行内公式解析。
- 验证纯公式块 `$$a_n=...$$` 仍独立显示。
- 验证 `\frac{项数}{2}`、`a_{中间项}` 等中文标签转换为 MathJax 可识别源码。
- 验证 `$...$`、`$$...$$`、重复及不对称美元输入不会再次重复包裹。
- 验证多个行内公式和跨富文本运行段公式仍保持正确顺序。

## 仍需真实 Obsidian 手工验证

- 使用截图中的两条内容复测，确认“通项公式”与公式同行，“求和公式”不再显示原始美元源码。
- 单独建立只包含 `$$...$$` 的文字块，确认仍保持独立居中。
- 在公式编辑器中分别粘贴 `$v=s/t$` 和 `$$v=s/t$$`，选择行内模式后确认只生成一层 `$...$`。

## 交付包验证

- 安装包 SHA-256：`0eee1463def99c93ea1d3325a800f955ba0cc82c9e093b8fff3e6299c1a23463`，已写入 `update.json`。
- 本轮交付后缀：`293283`。
