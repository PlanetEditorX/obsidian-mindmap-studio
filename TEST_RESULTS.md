# Test Results

版本：1.43.6

- 单元测试：**323 / 323 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**56 个源码模块、1128 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮根因

- 文章/通读右下角加载进度没有插件级开关，启用该版本后始终显示。
- “全部替换”直接复用界面中的 `renderedResults`；该数组受“单次最多显示结果”限制，最多只有 20–500 条，因此批量替换只能处理当前显示的第一批结果。
- 批量替换写回文件后只依赖文件监听器延迟更新索引，用户立即重新搜索时可能继续看到旧结果。

## 本轮实现

- 新增“视图与阅读 → 显示右下角加载进度”，默认关闭。关闭只隐藏提示，不跳过文章族上下文构建。
- “全部替换”在执行时重新查询当前全局或导图族范围内的完整结果集，不受界面显示上限影响。
- 替换文本不再自动 `trim()`，允许保留用户输入的首尾空格。
- 每个文件写回并校验后立即重建该文件搜索条目，再同步已打开视图。
- 新增批量替换开始、单文件完成/失败和最终完成调试事件。

## 自动回归

- 验证加载进度默认关闭、设置位于“视图与阅读”，并只在开关启用时挂载。
- 验证“全部替换”使用完整作用域查询，而非截断的可见结果数组。
- 验证批量写回后调用单文件索引即时刷新。
- 验证全部 323 项单元测试、综合回归、文档、仓库、类型与生产构建通过。

## 仍需真实 Obsidian 手工验证

- 保持默认设置打开文章和通读，确认右下角不显示加载进度；开启设置后确认显示并在 100% 后自动隐藏。
- 将“单次最多显示结果”设为 20，搜索超过 20 个匹配的关键词并执行“全部替换”，确认 20 条以外的节点也全部修改。
- 批量替换后立即用原关键词重新搜索，确认不会继续显示旧结果。

## 交付包验证

- 安装包 SHA-256：`8ffc3a539abbc6c9cc067f25deb0540c5a5eab6ecefb6a285746dac9f4191fdb`，已写入 `update.json`。
- 安装包：`obsidian-mindmap-studio-1.43.6-plugin-873313.zip`
- 源码包：`obsidian-mindmap-studio-1.43.6-source-873313.zip`
- Codex 交接包：`Codex-1.43.6-handoff-873313.zip`
