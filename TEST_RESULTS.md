# Test Results

版本：1.41.6

- 单元测试：**297 / 297 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1088 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮问题

- 单篇文章中心节点关闭编号后，通读错误继承了整页关闭状态，导致全书目录、章标题和正文标题全部失去编号。
- 通读模式处于只读状态时，工具栏“主题与外观”被通用编辑控件禁用，无法修改阅读主题和页面覆盖。
- 默认折叠、默认行号和两项自动阈值仍分散在独立“代码行为”分区，页面工具栏的“代码外观”也缺少自动阈值覆盖。

## 本轮实现

- 通读新增独立 `readingTocEntries` 与 `buildReadingArticleNodeInfo()`：只忽略每个物理导图中心节点的文章关闭状态，继续尊重普通节点关闭、手动层级与末端序号规则。
- 文章模式维持原语义：中心节点关闭后，当前物理导图的文章正文、文章目录、导出和子文章页标题不显示编号。
- 通读正文继续保持跨文件只读；“主题与外观”按钮脱离通用只读禁用集合，页面外观修改使用独立可撤销事务并恢复原通读位置。
- 全局代码设置统一归入“主题与外观 → 代码外观”；旧“代码行为”分区顺序映射到主题与外观，不丢失既有配置。
- 页面外观新增自动保持展开与自动显示行号阈值，代码表现按节点、当前页面、插件全局逐项覆盖。

## 自动回归

- 验证文章中心节点关闭时，文章仍无编号而通读恢复连续全书编号。
- 验证通读独立编号继续尊重普通节点关闭、手动编号和末端序号规则。
- 验证通读只读状态下仅开放主题与外观，不开放正文内容编辑。
- 验证全局不再出现独立“代码行为”分区，旧分区名称可兼容迁移。
- 验证页面代码自动阈值优先于全局阈值，节点显式设置仍具有最高优先级。

## 仍需真实 Obsidian 手工验证

- 将文章编号设为关闭后切换到通读，确认全书目录、章标题、节标题和末端序号恢复且连续。
- 在通读锁定状态打开“主题与外观”，修改主题、阅读外观和目录深度，确认保存后仍停留在原章节附近。
- 在插件设置确认“代码行为”不再单独出现，相关项目集中在“主题与外观 → 代码外观”。
- 分别设置全局和当前页面的自动展开、自动行号阈值，确认页面覆盖生效；节点显式设置仍优先。

## 交付包验证

- 安装包 SHA-256：`982eaf809d5542d63a4925d4c43b70b53773a3db0a2138a9f2718bff1fe07d9e`，已写入 `update.json`。
- 本轮交付后缀：`419343`。
