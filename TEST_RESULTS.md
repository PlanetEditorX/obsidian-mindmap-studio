# Test Results

版本：1.41.5

- 单元测试：**294 / 294 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1086 个具名声明**
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

## 本轮问题与日志依据

- 用户在“主题与外观”中将文章编号方式设为关闭并应用后，文章立即重绘，但旧编号构建仍从一级子节点按自动层级生成“第一章”。
- 根节点本身从不显示文章编号，旧实现只把 `none` 应用到根节点自身，因此这个设置在视觉上等同于没有生效。

## 本轮实现

- 新增 `isDocumentArticleNumberingDisabled()`，把中心节点的 `articleNumberingMode: "none"` 解释为当前物理导图的整页编号开关。
- `buildArticleNodeInfo()` 在整页关闭时不生成章节标签、末端下一层序号或带圈序号，但继续计算真实标题深度和结构。
- 递归文章目录构建复用同一关闭状态，目录项不再回退成“第一章、第一节”。
- 子文章页标题在当前文档关闭编号时不再显示来自分页导航的父级编号。
- 文章、通读、Markdown/HTML/Word 等导出继续共用 `buildArticleNodeInfo()`，因此关闭语义一致。
- 普通非根节点的 `none` 保持原有语义：只跳过自身，不占用同级序号，后代继续按结构编号。
- 设置说明明确区分中心节点整页关闭与普通节点单点跳号。

## 自动回归

- 验证中心节点关闭后，本文件所有章节和末端序号为空，标题深度与正文顺序不变。
- 验证开启末端带圈编号时，整页关闭仍不会生成任何自动序号。
- 验证普通节点关闭只跳过自身，其子标题仍继续生成下一层编号。
- 验证递归目录和子文章页标题都读取统一的整页关闭状态。
- 保留全局页面过渡、表格列宽、文件名保护、阅读恢复和导出专项测试。

## 仍需真实 Obsidian 手工验证

- 在“主题与外观 → 文章编号与目录”把编号方式设为关闭，确认当前文章正文不再显示“第一章”等编号。
- 切换到通读模式，确认当前物理导图章节不显示编号，目录层级和跳转仍正常。
- 当前文件存在父级分页导航时，确认文章页标题前不再显示父级生成编号。
- 将普通非根节点单独设为关闭，确认只隐藏该节点编号，子级仍继续编号。
- 重新设为自动或手动层级，确认编号立即恢复且同级计数从正确位置开始。

## 交付包验证

- 安装包 SHA-256：`5851cb020dcc5eba4918cce63354ae665ca5ab2598b142a6a1cda6ef719565c2`，已写入 `update.json`。
- 本轮交付后缀：`135633`。
