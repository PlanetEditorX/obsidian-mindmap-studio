# Test Results

版本：1.43.7

- 单元测试：**323 / 323 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**56 个源码模块、1130 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮根因

- 编辑器此前只处理 `Ctrl/Cmd+Alt+F`，并明确把 `Ctrl/Cmd+F` 留给 Obsidian。
- 用户删除 Obsidian 的“搜索当前文件”绑定后，没有任何插件入口接管标准查找快捷键，因此按键无响应。
- 仅在编辑器根节点监听也不够可靠：焦点位于视图外层或某些编辑控件时，事件可能不会经过根节点。

## 本轮实现

- 活动视图为 MindMap Studio 时，插件在窗口捕获阶段直接处理 `Ctrl/Cmd+F`，打开当前父子导图族搜索。
- 搜索不再依赖 Obsidian 的“搜索当前文件”命令绑定是否存在。
- 可配置的全局搜索快捷键优先处理；弹窗内部按键不重复打开搜索。
- 编辑器根节点保留回退处理，旧版 `Ctrl/Cmd+Alt+F` 继续兼容。
- 使用 `KeyboardEvent.code` 兼容非英文键盘布局，并保持编辑控件聚焦时可触发。

## 自动回归

- 验证 `Ctrl/Cmd+F` 在可编辑控件过滤之前被处理。
- 验证窗口捕获层只在活动视图为 MindMap Studio 时接管标准查找快捷键。
- 验证可配置全局搜索快捷键保持优先，弹窗内不会重复截获。
- 验证旧版 `Ctrl/Cmd+Alt+F` 仍由编辑器回退路径支持。
- 验证全部 323 项单元测试、综合回归、文档、仓库、类型与生产构建通过。

## 仍需真实 Obsidian 手工验证

- 删除 Obsidian 的“搜索当前文件”快捷键绑定，在导图、大纲、文章和通读模式分别按 `Ctrl+F`，确认打开当前父子导图族搜索。
- 在节点编辑框、文章正文编辑和标题编辑状态下按 `Ctrl+F`，确认仍能打开搜索。
- 在搜索弹窗的输入框中再次按 `Ctrl+F`，确认不会叠加打开第二个弹窗。
- 验证默认 `Ctrl+Shift+F` 仍打开全局搜索，旧版 `Ctrl+Alt+F` 仍可打开当前导图族搜索。

## 交付包验证

- 安装包 SHA-256：`1dacc7b953b0d2eaee68ce2b1fb0d11cff7351124ecddaf21eec2322a7b0a4e5`，已写入 `update.json`。
- 安装包：`obsidian-mindmap-studio-1.43.7-plugin-603627.zip`
- 源码包：`obsidian-mindmap-studio-1.43.7-source-603627.zip`
- Codex 交接包：`Codex-1.43.7-handoff-603627.zip`
