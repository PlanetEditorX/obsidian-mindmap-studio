# MindMap Studio 1.15.2 文章目录与当前脑图层级覆盖测试结果

## 本次修复

- 修复文章模式目录只在存在子导图时启用的问题。现在最顶层脑图只要存在可识别的标题目录项，即使是单个 `.mindmap` 文件也会显示目录。
- “原始文章”正文页顶部保留目录；工具栏仍可切换到纯目录页。
- “当前脑图外观 → 文章编号与目录”新增“目录最大层级”：默认跟随插件全局设置，手动选择 1–8 层后，当前 `.mindmap` 文件优先。
- 文件级目录层级同时作用于文章模式目录与通读模式全书目录；改回“跟随插件设置”会删除文件级覆盖。
- 继续使用独立的 `tocDepth` 过滤目录，手动编号从第 5 级开始时不会再次造成目录为空。

## 数据兼容

- 新增可选字段 `view.articleTocMaxDepth`，范围为 1–8。
- 旧文件缺少该字段时自动跟随插件全局 `articleTocMaxDepth`，无需迁移。
- 序列化、重新打开和非法边界钳制均有自动测试。

## 验证结果

- `npm test`：通过。
- 文档覆盖检查：通过，27 个 TypeScript 模块、505 个命名声明均有 JSDoc。
- `npm run docs:generate`：通过，已更新 `docs/FUNCTION_REFERENCE.md`。
- `npm run build`：通过，已从当前 TypeScript 源码重新生成 `main.js`。
- `node --check main.js`：通过。
- Obsidian API 运行时替身加载冒烟检查：通过，默认导出为 `MindMapStudioPlugin`。
- 安装包完整性与 SHA-256 校验：通过。

## 关键回归覆盖

- 单脑图存在章节标题、没有子导图时，文章模式仍启用目录。
- 文章正文状态仍渲染目录，纯目录状态保持原有切换能力。
- 未设置文件覆盖时使用插件全局目录层级。
- 文件覆盖优先于插件全局设置，并同时控制文章和通读目录。
- 文件覆盖可恢复为“跟随插件设置”。
- 第 5 级编号 `1.` 对应目录结构第 1 层，目录最大层级为 3 时仍显示。
