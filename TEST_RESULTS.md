# MindMap Studio 1.15.2 文章模式目录还原测试结果

## 本次修改

- 还原文章模式原有目录交互：只有最上层脑图存在子导图时才启用文章目录页。
- 工具栏继续在“纯目录页”和“原始文章”之间切换。
- “原始文章”恢复为仅显示正文，不再在正文顶部直接插入目录。
- 单个脑图即使存在可识别的章节标题，也不会自动进入文章目录页。
- 保留“当前脑图外观 → 文章编号与目录 → 目录最大层级”覆盖设置。
- 当前脑图目录层级覆盖仍优先于插件全局设置，并继续同时用于文章目录页和通读模式全书目录。
- 保留编号层级与目录结构层级分离，手动编号从第 5 级开始时通读目录不会为空。

## 数据兼容

- `view.articleTocMaxDepth` 可选字段继续保留，范围为 1–8。
- 旧文件缺少该字段时自动跟随插件全局 `articleTocMaxDepth`，无需迁移。
- 本次只还原文章模式渲染入口和页面结构，不修改 `.mindmap` 文件格式。

## 验证结果

- `npm run docs:generate`：通过，已重新生成 `docs/FUNCTION_REFERENCE.md`。
- `npm test`：通过。
- 文档覆盖检查：通过，27 个 TypeScript 模块、505 个命名声明均有 JSDoc。
- `npm run build`：通过，已从当前 TypeScript 源码重新生成 `main.js`。
- `node --check main.js`：通过。
- Obsidian API 运行时替身加载冒烟检查：通过。

## 关键回归覆盖

- 顶层脑图没有子导图时，文章目录入口保持关闭。
- 顶层脑图存在子导图时，文章目录入口正常启用。
- 目录状态只渲染纯目录页并提前返回。
- 原始文章状态只渲染正文，不再调用内嵌目录渲染。
- 文件级目录最大层级覆盖仍优先于插件全局设置。
- 通读模式仍使用文件级目录最大层级和独立 `tocDepth`。
