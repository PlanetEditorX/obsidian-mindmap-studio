# Test Results

版本：1.39.3

- 单元测试：282 / 282 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1061 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 文章与通读的带圈序号从第 1 项开始传递十进制索引，不再直接把 `①–㊿` 字形放入伪元素；全部序号使用当前正文字体，避免 Windows 字体回退造成偏小、偏细或基线错位。
- CSS 使用固定首行编号槽、统一圆环尺寸、600 字重、表格数字宽度和绝对定位；第 1、20、50、51、67 项共用同一视觉规则，多行正文继续从文字起点换行。
- HTML 导出对第 1、51、67 项均输出 `.circled-number` 结构，并使用继承字体与稳定基线；Markdown 和 Word 仍在 1–50 使用 Unicode `①–㊿`，51+ 使用 `◯51` / `◯67` 可读回退。
- 文章缓存渲染修订提升为 `article-node-cache-v4`，1.39.2 的旧节点 HTML 会自动失效，避免升级后恢复 Unicode 字形或出现双圈。
- 原有 67 个同级节点、Unicode 1/20/21/35/36/50 映射、第 9 级末端正文和页面级样式覆盖继续通过。
- 1.39.1 的深层标题编号边界、1.39.0 的文件浏览器增量筛选与文章缓存写入合并、1.38.9 的设置保存合并及 1.38.8 的状态记忆继续通过完整回归。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、`.tmp/`、嵌套 ZIP 和本轮未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.39.3-handoff-351281.zip`。
- Codex ZIP 的中文目录和文件名使用标准 UTF-8 主文件名和 general-purpose bit 11；Linux Info-ZIP 实际解压后不得存在字面量 `#Uxxxx`。
- 安装 ZIP SHA-256：`8aa6bb61c2df66a97ade9646904284b5fe9574fafbc4ac95c037b37061256c52`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在 Win10 中切换到“带圈数字（统一圆圈，支持 51+）”，检查第 1、9、10、20、50、51、67 项的圆圈直径、数字粗细、首行基线和正文换行缩进。
- 分别在文章与通读模式中测试普通、加粗、不同字号和不同文章主题，确认编号跟随正文颜色与字体，不再调用 `Segoe UI Symbol` 等符号字体。
- 重开 1.39.2 曾缓存的文章，确认旧缓存自动失效且不会出现双圈；随后再次打开确认新缓存恢复正常。
- 检查 HTML 导出的第 1/50/51 项视觉一致性，以及 Markdown/Word 中 Unicode 与 `◯51` 回退仍可读。
