# Test Results

版本：1.41.7

- 单元测试：**297 / 297 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1088 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮问题

- 提取为子导图后，在导图模式点击“返回父导图”仍调用文章目录导航，导致目标页出现白色文章解析卡片，并最终落到文章模式目录。
- 当前页面同时保留黑色全局过渡与白色文章加载状态，造成重复提示。

## 本轮实现

- 顶部父级导航按当前模式分流：仅文章模式使用父目录意图；导图、大纲、通读和题库模式直接打开父导图并聚焦原挂载节点。
- 非文章模式返回只保留黑色统一页面过渡，不再创建白色文章目录解析状态。
- 文章模式的返回上一级、分页返回和 Esc 返回目录行为保持不变。

## 自动回归

- 验证非文章模式父级返回调用普通导图导航并传递父挂载节点 ID。
- 验证文章模式仍调用目录意图，不改变既有章节阅读流程。
- 验证统一页面过渡继续覆盖父子导图导航。

## 仍需真实 Obsidian 手工验证

- 从父导图提取分支进入子导图，保持导图模式点击顶部返回：只出现黑色过渡，返回后仍为导图模式并选中原挂载节点。
- 在文章模式从子导图返回，确认仍进入父导图目录。

## 交付包验证

- 安装包 SHA-256：`dc5fdcc08ef09ac5fea4c35532757515d15538fd2e9723700c7c46fbaf6270de`，已写入 `update.json`。
- 本轮交付后缀：`089571`。
