# Test Results

版本：1.43.9

- 单元测试：**325 / 325 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**56 个源码模块、1131 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮根因

- 从文章正文切到导图或大纲后，返回文章时没有在文章重绘前声明当前模式的语义节点为显式目标。
- 隐藏但仍挂载的旧文章 DOM 和旧像素滚动值会先参与正文窗口恢复；程序化滚动又会触发文章顶部边缘扩展，导致反复加载前文直至页首。

## 本轮实现

- 当文章正文暂时切到导图或大纲再返回时，将当前节点规范化为 `pendingArticleFocusLocation`，让本轮文章入口窗口直接围绕该节点渲染并定位。
- 首次进入顶层文章仍按原规则显示目录；只有已处于正文状态的临时模式往返才恢复正文。
- 活跃语义恢复事务期间暂停文章窗口边缘自动扩展；用户滚轮、触摸、指针或键盘接管后恢复正常加载。

## 自动回归

- 新增文章 → 导图/大纲 → 文章的当前节点恢复契约。
- 验证首次进入顶层文章仍默认显示目录。
- 验证程序化语义恢复期间不会触发连续“加载前文”。
- 验证全部 325 项单元测试、综合回归、文档、仓库、类型与生产构建通过。

## 仍需真实 Obsidian 手工验证

- 在长文章中滚动到中段，切到导图模式修改当前节点，再切回文章，确认直接定位当前文字且不连续加载前文。
- 在导图模式选择另一个节点后返回文章，确认定位新选择的节点。
- 启用文章目录时，从新打开的顶层文件首次进入文章仍显示目录；从正文临时切换导图再返回则恢复正文。

## 交付包验证

- 安装包 SHA-256：`5b04317669f81b692a92d5ece4f9b72b2a72e3a7d491f4f198f23f77783ee698`，已写入 `update.json`。
- 安装包：`obsidian-mindmap-studio-1.43.9-plugin-501507.zip`
- 源码包：`obsidian-mindmap-studio-1.43.9-source-501507.zip`
- Codex 交接包：`Codex-1.43.9-handoff-501507.zip`
