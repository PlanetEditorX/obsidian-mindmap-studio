# Test Results

版本：1.39.5

- 单元测试：283 / 283 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1061 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 根据 Win10 截图确认：带圈正文首字已经与普通圆点正文对齐，但圆圈右边缘贴住正文，视觉上形成“①正文”粘连。
- 保持自动对齐正文起点不变，将圆圈由 1.65em 收敛为 1.34em，边框由 0.11em 调整为 0.08em，并同步垂直位置；数字字号仍为 0.86em，1 位和 2 位编号均保持清晰。
- 自动模式将圆圈向编号区内补偿 0.13em，正文不移动；Chromium 实际布局中普通圆点与带圈正文首字均位于 x=90 px。
- Chromium 实际布局中圆圈右边缘与正文间距约 4.19 px，圆圈中心仅位于普通圆点中心左侧约 2.11 px，在保持文字对齐的同时提供清晰间隔。
- CSS 几何契约直接校验：两类正文起点相等、圆圈与正文间距不少于 0.18em、圆圈中心向左偏移不超过 0.15em。
- “顶格”模式、普通下一级文章序号、1–50/51+ 带圈编号、深层第 9 级末端正文、HTML/Markdown/Word 导出和文章缓存 v4 行为保持不变。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、临时目录、嵌套 ZIP和本轮未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.39.5-handoff-826973.zip`。
- Codex ZIP 的中文目录和文件名使用标准 UTF-8 主文件名和 general-purpose bit 11；Linux Info-ZIP 实际解压后不得存在字面量 `#Uxxxx`。
- 安装 ZIP SHA-256：`425507abe6cdfe5b7a754b4c050c23e08d8c617239491feb7c5c0bb6727a77df`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在截图对应的 Win10 文档中确认圆圈与正文之间已有明显空隙，同时带圈正文首字仍与上方普通圆点正文对齐。
- 检查 1、9、10、50、51、67 以及多行正文，确认圆圈尺寸、数字字重、垂直基线和换行悬挂缩进。
- 分别测试“自动（与上级标题对齐）”与“顶格”；顶格模式应保持原有位置。
