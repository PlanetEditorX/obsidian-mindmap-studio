# Test Results

版本：1.37.1

- 单元测试：231 / 231 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（51 个源码模块、991 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 导图和大纲模式粘贴图片时，目标节点只读取粘贴瞬间的 `selectedId`，不再优先使用可能残留的旧 DOM 焦点节点。
- 文章和通读模式继续按实际事件目标与活动内容块插入图片，不改变原有段落后插入语义。
- “编辑节点内容”弹窗提供“+ 粘贴图片”和图片块内“粘贴剪贴板图片”按钮。
- 弹窗可直接接收 `Ctrl/Cmd+V` 图片粘贴；按钮读取剪贴板权限受限时会明确提示改用快捷键。
- 弹窗粘贴图片先保存到仓库，再写入当前弹窗对应节点，并按节点 ID 与图片块 ID进入现有自动上传排程。
- 规范 UTF-8 中文示例路径保持可读。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、Codex 和嵌套 ZIP。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.37.1-handoff-553310.zip`。
- 中文路径使用 ZIP UTF-8 文件名标志，并执行实际解压验证。
- 安装 ZIP SHA-256：`a12a210058c1e07cbe1fd95dcee01d390fd9f033f03a3247327adc4de30014a6`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 先单击节点 A，再单击节点 B 并粘贴外部图片，确认图片只进入节点 B；重复测试旧节点仍有文本焦点的情况。
- 打开“编辑节点内容”，分别使用按钮和 `Ctrl/Cmd+V` 粘贴图片，确认图片保存、预览、自动保存和自动上传排程正常。
