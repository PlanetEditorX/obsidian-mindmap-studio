# Test Results

版本：1.41.4

- 单元测试：**291 / 291 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1085 个具名声明**
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

## 本轮问题与日志依据

- 大型子导图打开时，日志记录文件体积约 716 KB；旧流程会立即解析和渲染，没有用户可见的准备状态。
- “合并回主导图”从菜单点击直接进入父导图导航，克隆、序列化、保存和视图切换期间没有阶段提示，主线程繁忙时会表现为界面卡死。
- 旧导航链发生异常时可能产生未处理 Promise 拒绝，遮罩或页面状态没有统一收尾。

## 本轮实现

- 新增统一页面过渡层，提供状态图标、标题、说明、`role=status`、`aria-live` 与 `aria-busy`。
- 过渡层先等待两个动画帧完成实际绘制，再执行大文件克隆、序列化、保存或目标页面渲染。
- 合并回主导图分阶段显示“正在合并回主导图”“正在打开主导图”。
- 提取为子导图分阶段显示“正在提取为子导图”“正在更新主导图”“正在打开子导图”。
- 创建或打开子导图、父子导图返回、文章目录导航、导图链接和大纲链接统一经过页面过渡。
- 导图、大纲、文章、通读、题库等显示模式切换统一显示目标模式状态，完成后目标页面淡入。
- 导航异常统一关闭过渡层并提示“页面切换失败，请稍后重试”，避免未处理拒绝和永久忙碌状态。
- 系统启用“减少动态效果”时取消脉冲、淡入和过渡时长，保留静态状态文字。

## 自动回归

- 验证提取和合并回调前已完成过渡层绘制。
- 验证创建、打开及返回父子导图均通过统一导航过渡。
- 验证所有显示模式使用统一切换状态并在完成后执行目标页进入动画。
- 验证页面过渡具备语义状态、忙碌属性和减少动态效果回退。
- 验证异常导航会清理遮罩、移除 `aria-busy` 并给出错误提示。
- 保留上一轮表格列宽、文件名保护和无横向滚动专项测试。

## 仍需真实 Obsidian 手工验证

- 在 700 KB 以上子导图执行“合并回主导图”，确认状态层先出现，并依次显示合并与打开阶段。
- 对大型分支执行“提取为子导图”，确认创建文件、更新主图和打开子图期间始终有可见状态。
- 连续切换导图、大纲、文章、通读和题库，确认过渡不会闪烁、残留或吞掉目标滚动位置。
- 通过父级面包屑、返回按钮、文章目录和子导图节点往返，确认错误时遮罩能够退出。
- 在系统“减少动态效果”开启时确认仅显示静态状态，不执行脉冲与淡入。

## 交付包验证

- 安装包 SHA-256：`a9d995409afd72b7e92efe4186fa271fe146980e90ace3464a8e3e2ea2afc766`，已写入 `update.json`。
- 本轮交付后缀：`082454`。
