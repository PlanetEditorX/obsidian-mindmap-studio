# Test Results

版本：1.38.3

- 单元测试：259 / 259 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（51 个源码模块、992 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 编辑器以文章、大纲或通读模式启动时，不再预先计算完整思维导图布局；仅在实际进入导图模式时执行布局。
- 单次布局过程使用 `WeakMap` 缓存每个节点的子树高度，避免深层链式节点被重复递归计算。
- 1200 层深链性能回归通过，完整布局耗时阈值固定为 500 ms 以内。
- 本地基准中，1500 层链式布局由约 1498 ms 降至约 12 ms；该数值用于开发环境对比，不作为不同电脑上的绝对承诺。
- 文章首批正文渲染完成后立即替换加载骨架并显示，剩余章节继续通过 `requestAnimationFrame` 分帧填充。
- 分帧过程中保留当前可见文章页和视口位置，不再要求等全部节点完成后才显示整页。
- 文章渲染取消、重复打开与模式切换继续清理过期任务，避免旧批次覆盖新页面。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、Codex、临时目录、嵌套 ZIP和未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.38.3-handoff-787083.zip`。
- 安装 ZIP SHA-256：`cf88c4c7fdfd9d52e6a04044e46e50438a9988aba44ee49610c0aca8a05b87e1`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 使用原先需要接近半分钟打开的约 80 KB 文件，分别执行首次打开和关闭后重新打开，记录标题/首段可见时间与全部节点补齐时间。
- 首批正文出现后连续滚动，确认剩余占位内容平滑补齐且阅读位置不跳动。
- 切换到思维导图模式，确认全部节点、连接线、折叠状态和布局结果与优化前一致。
- 在 Win10 与实际主题、第三方插件组合下观察开发者控制台，确认没有新的长任务、异常或渲染取消错误。
