# Test Results

版本：1.35.3

- 单元测试：216 / 216 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（50 个源码模块、958 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 截图与截图并识别继续使用独立命令、快捷键、工具栏和节点右键入口。
- Windows 使用 Per-Monitor V2 DPI 感知抓取完整虚拟桌面，并返回全部显示器、主屏和鼠标所在屏幕元数据。
- 截图编辑器提供由页面本地生成的“全部屏幕 / 屏幕 N”切换，忽略 PowerShell 返回的显示器标签，避免 Windows PowerShell 5.1 编码乱码。
- 截图背景按显示器宽高比完整适配，不再强行拉伸；导出使用虚拟桌面原始 PNG 像素映射。
- 选区使用圆角蓝色细描边、柔和阴影和圆形八方向手柄；左上角显示全局位置与尺寸。
- 几何图形支持矩形、圆角矩形、椭圆、填充、颜色和线宽；箭头可切换为直线；画笔、箭头、直线、文字与序号支持颜色及线宽。
- 文字使用画布内 `textarea`，通过延迟稳定聚焦和事件隔离避免同一次鼠标释放立即失焦，支持中文输入法、多行输入及 `Ctrl/Cmd + Enter` 完成。
- 截图编辑器已删除“固定”动作及所有原生置顶窗口实现。
- 截图编辑器打开或切换屏幕后，默认选区覆盖当前屏幕完整范围。
- 普通截图在鼠标释放后保持打开并显示完整标注与输出工具栏；截图并识别保留完整工具栏/样式栏 DOM，但以 `opacity: 0` 和 `pointer-events: none` 完全隐藏，避免空节点中断脚本。倒计时沿用原有小号胶囊和“秒后自动识别”文案，只把小数秒改为整数 `3、2、1`；悬停选区边框、拖动条或缩放手柄时暂停，离开边框或重新框选完成后从 3 秒重启。Esc 同时在 iframe 与宿主窗口上注册。
- 普通截图和截图并识别生成的内嵌脚本均通过语法编译测试；识别模式专项验证完整透明控件树存在，避免空工具栏访问异常导致倒计时与 Esc 监听未注册。
- 已删除系统交互式区域截图的静默回退，抓取或覆盖层创建失败时返回明确错误。
- 桌面 Node/Electron API 继续动态加载，移动端插件加载契约保持通过。

仍需真实 Obsidian 桌面端手工冒烟：

- Windows 双屏/多屏中切换“全部屏幕”和每块显示器，检查负坐标、不同分辨率及混合 DPI。
- macOS 屏幕录制权限、Linux 可用抓屏命令，以及负坐标多显示器布局。
- 100%、125%、150%、200% 及混合 DPI 下检查背景无拉伸、选区坐标与导出原始像素一致。
- 真实 AI 视觉接口与本地 Tesseract OCR。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、临时目录和嵌套 ZIP。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`。
- 中文示例和中文文档路径使用 ZIP UTF-8 文件名标志，并执行实际解压验证。
- 安装 ZIP SHA-256：`832591032470c24dc21620ee11b4106cf23b2222afddddb765d52f86d1e083f6`，已写入 `update.json`。
