# Test Results

版本：1.41.8

- 单元测试：**300 / 300 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1088 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮问题

- 通读目录按跨文件结构生成了正确编号，但正文在每个子导图内再次独立判断标题，导致修改层级后目录显示“一、”等序号，具体章节正文仍无编号。
- 顶部工具栏同时使用 Obsidian 提示与浏览器原生 `title`，悬停时出现黑底和白底两个说明窗。
- “打开节点链接”作为重复命令占用工具栏和右键菜单；折叠与适应画布在非导图模式仍显示。

## 本轮实现

- 通读正文按 `filePath + nodeId` 复用全书目录的层级、标题判定和编号，目录与正文保持一致。
- 删除“打开节点链接”工具栏项、配置项和右键菜单命令；保留节点内链接与富文本链接的直接点击兼容。
- 顶部工具栏只保留 `aria-label`，移除浏览器原生 `title`。
- 展开/收起、批量折叠和适应画布仅在导图模式显示。

## 自动回归

- 验证跨文件目录将本地正文节点判为标题时，通读正文同步显示同一层级与“一、”编号。
- 验证工具栏配置和编辑器源码不再包含“打开节点链接”命令。
- 验证工具栏按钮不再设置原生 `title`，并按模式隐藏导图专用操作。

## 仍需真实 Obsidian 手工验证

- 在通读模式修改文章层级，确认目录与正文标题同时显示相同编号。
- 悬停顶部工具栏按钮，确认只出现黑底说明。
- 在文章、大纲、通读和题库模式确认折叠与适应画布按钮隐藏。
- 打开包含既有节点链接的旧导图，确认节点内链接仍可直接点击。

## 交付包验证

- 安装包 SHA-256：`53f10774f08263ced7a3d6064464522f37f8f36a833e5959ac028ff894051426`，已写入 `update.json`。
- 本轮交付后缀：`393800`。
