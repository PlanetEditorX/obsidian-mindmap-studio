# MindMap Studio 1.15.2 同层文章分页修复测试结果

## 本次修改

- 文章页底部上一篇/下一篇不再使用递归全书目录的相邻项。
- 分页候选改为当前物理 `.mindmap` 页在同一父级、同一目录结构层级下的兄弟子导图。
- 进入“第一章 世界”后，下一篇直接打开“第二章 中国”，跳过当前页面内部的“第一节 世界历史”“第二节 四大文明古国”。
- 如果当前物理页面位于节级，则只在同一章下的节级兄弟页面间切换。
- 子导图文章页顶部显示父目录生成的完整编号标题，例如“第一章 世界”，不再只显示“世界”。
- 标题编号为生成内容；行内编辑只作用于当前子导图根节点文字，避免把“第一章”等编号写回文档。
- 保留文章模式“纯目录页 / 原始文章”原有切换、通读目录、文件级目录最大层级覆盖和导图视口恢复。

## 数据兼容

- 不修改 `.mindmap` 文件格式、数据版本或持久化字段。
- 递归目录仍保留全部页内标题，仅运行时分页候选改为物理兄弟页面。
- 旧父子导图无需迁移或重新创建。

## 验证结果

- `npm run docs:generate`：通过，已重新生成 `docs/FUNCTION_REFERENCE.md`。
- `npm test`：通过。
- 文档覆盖检查：通过，27 个 TypeScript 模块、509 个命名声明均有 JSDoc。
- `npm run build`：通过，已从当前 TypeScript 源码重新生成 `main.js`。
- `node --check main.js`：通过。
- Obsidian API 运行时替身加载冒烟检查：通过。

## 关键回归覆盖

- 章级页面分页列表只包含“第一章 世界、第二章 中国”，不包含当前文件内部的节节点。
- 节级子页面只在相同父章节的节级物理页面之间切换，不串到其他章节。
- 顶层总目录文件继续显示自身中心节点标题，不误显示第一章标题。
- 子导图页面可解析并显示完整编号目录标题。
- 选择或定位页内节点不会改变当前物理页面的上一篇/下一篇目标。
