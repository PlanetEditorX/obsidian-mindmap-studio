# Test Results

版本：1.39.4

- 单元测试：283 / 283 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1061 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 根据 Win10 截图定位到自动对齐的带圈编号被通用 `.mms-article-leaf-numbered` 规则清除了 `margin-inline-start`，导致圆圈中心比普通圆点向左约 22 px，正文首字向左约 11 px。
- 自动对齐的带圈编号现在恢复 1.25em 上级标题缩进，并把编号槽从 1.9em 收敛为 1.25em；圆圈在槽内向左补偿 0.15em，不改变圆圈直径、字重和垂直基线。
- Chromium 实际布局验证：普通圆点正文与带圈编号正文首字起点完全一致；圆点中心与圆圈中心误差约 0.01em，小于 1 px。
- 新增 CSS 几何契约测试，直接校验两类标识的正文起点相等、标识中心误差小于 0.02em。
- “顶格”模式、普通下一级文章序号、1–50/51+ 带圈编号、深层第 9 级末端正文、HTML/Markdown/Word 导出和文章缓存 v4 行为保持不变。
- 1.39.3 的统一字体与圆环、1.39.1 的八级编号边界、1.39.0 的增量筛选和缓存写入合并、1.38.9 的设置保存合并及 1.38.8 的状态记忆继续通过完整回归。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、临时目录、嵌套 ZIP和本轮未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.39.4-handoff-064572.zip`。
- Codex ZIP 的中文目录和文件名使用标准 UTF-8 主文件名和 general-purpose bit 11；Linux Info-ZIP 实际解压后不得存在字面量 `#Uxxxx`。
- 安装 ZIP SHA-256：`f08fec25c88cb169e9a25c372d833fdd6b1445faa5fe2f80b386798cd5489a0e`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在截图对应的 Win10 文档中确认“（六）”下普通圆点与“（七）”下带圈数字位于同一标识列，且两组正文首字形成同一垂直线。
- 分别测试“自动（与上级标题对齐）”和“顶格”，确认只有自动模式增加上级标题缩进，顶格模式保持原行为。
- 检查 1、9、10、50、51、67 以及多行正文，确认圆圈尺寸、垂直基线和换行悬挂缩进不变。
