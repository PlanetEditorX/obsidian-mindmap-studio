# MindMap Studio 1.15.2 自定义最高层级与视口保持测试结果

## 已完成

- `npm test` 通过：模型、文章编号、布局、搜索、导入导出及源码回归测试全部通过。
- 文档覆盖检查通过：27 个 TypeScript 模块中的 502 个命名声明均具备 JSDoc。
- `npm run build` 通过：TypeScript 类型检查成功，并重新生成 `main.js`。
- `main.js` 通过 `node --check` JavaScript 语法检查。
- `main.js` 使用 Obsidian API 运行时替身完成加载冒烟测试，默认插件导出为 `MindMapStudioPlugin`。
- 已重新运行 `npm run docs:generate`，更新 `docs/FUNCTION_REFERENCE.md`。

## 本次重点行为测试

- 从导图模式切换到大纲、文章或通读模式前，会把当前 `zoom / panX / panY` 写入文档视图状态；返回导图时恢复原视口，不再无条件调用自适应画布。
- 滚轮缩放、触控板平移、鼠标拖动画布、双指缩放/平移和定位节点都会标记为有效用户视口状态。
- 全局默认显示模式变化与模式按钮切换共用同一套视口保存/恢复逻辑。
- “当前脑图外观”和中心节点完整编辑窗口共用“文章编号方式 / 最高文章层级”控件。
- 文案统一为“手动层级（自定义最高层级）”。
- 中心节点设置最高层级 5 后，一级主题直接生成 `1.相得益彰`，不会额外下移到第 6 级。
- 同级存在自然标题时，第一个末端同级节点会正确生成 `（1）词义`，后续依次生成 `（2）易混淆成语`、`（3）区分`。
- 下一个一级主题生成 `2.方兴未艾`，其子级编号重新从 `（1）` 开始。
- 手动最高层级只覆盖文章层级，不会把孤立末端正文强制变成标题。
- 不同有效文章层级使用独立计数器；文章正文、递归目录、通读模式和 HTML/Word/PDF 导出共用同一套层级解析规则。
- 旧版 `skipArticleNumbering: true` 继续兼容为关闭编号，现有 `.mindmap` 文件无需批量迁移。

## 环境说明

上传的源码包未包含 `node_modules`，且当前依赖仓库暂时不可用。本次验证使用与锁文件版本兼容的隔离依赖完成；临时依赖未写入交付源码包。在正常联网的开发环境中，可执行 `npm ci && npm test && npm run build` 复现。
