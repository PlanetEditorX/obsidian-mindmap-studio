# Test Results

版本：1.39.13

- 单元测试：281 / 281 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（54 个源码模块、1047 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 显式章节目标在文章上下文未准备完成时也优先于“显示目录”，直接进入正文目标窗口；异步上下文不能把本次导航覆盖回目录或页首。
- 物理文件切换原子提交新文档与路径选项，并取消上一文件的阅读恢复、入口帧、窗口扩展和布局观察；旧事务不能继续写入新页面滚动位置。
- 返回父导图会先校验 `parentNodeId`；ID 缺失、过期或旧父路径信息不完整时，按来源子导图路径扫描目标父导图，恢复真实挂载节点。
- 调试模式从本次启动会话开始记录点击、按键、滚动、文件/视图切换、目录/父级导航、文章上下文、目标窗口、语义滚动事务和异常；日志有界并对高频事件节流。
- “复制 MindMap Studio 调试记录”输出带环境元数据的 JSONL；可编辑正文、输入值、标题属性、图床密钥和 AI 请求正文不进入调试日志。
- 原有目录首帧、双向骨架、5 KB 目标窗口、独占导航事务、布局稳定、边缘扩展、编辑与导出专项保持通过。

交付包验证：

- 源码 ZIP 第一层固定为 `obsidian-mindmap-studio/`，安装 ZIP 固定为 `mindmap-studio/`，Codex ZIP 固定为 `Codex/`。
- 三份 ZIP 的非 ASCII 路径使用 UTF-8 主文件名与 general-purpose bit 11，并执行实际解压验证。
- 安装包 SHA-256：`5570a8cf69fbc6852e459028ad2ec473aaa97c7a64ef19c9a291b51a627deac3`，已写入 `update.json`。
- 本轮交付后缀：`333629`。

仍需真实 Obsidian 手工验证：

- 在 Win10 Obsidian 中从深层子导图分别使用底部“返回上一级”、顶部父级导航和 `Esc`，验证有效、缺失及过期 `parentNodeId` 均能落到父挂载节点。
- 将顶层文章配置为“显示目录”，从目录连续点击当前文件章节和跨文件深层章节，确认显式目标始终直接进入正文且不回页首。
- 快速连续点击三个章节，等待图片、表格、代码和字体加载完成，确认只有最后目标生效且不发生二次滚动。
- 开启调试模式后完整复现一次问题，在命令面板执行“复制 MindMap Studio 调试记录”，确认可以直接粘贴 JSONL，且内容中没有正文、输入文字、Token 或密钥。
- 连续双向滚动跨越多个 5 KB 窗口，确认边缘流光不遮挡正文，向上补载不改变当前段落屏幕位置。
