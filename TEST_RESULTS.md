# Test Results

版本：1.38.8

- 单元测试：267 / 267 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（52 个源码模块、1036 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 设置页一级分类默认全部收起，不再默认展开“主题与外观”。
- 用户手动展开或收起一级分类时，会把当前展开分类列表写入 `settingsExpandedSections`；关闭并重新打开设置页后恢复上次状态。
- 设置搜索导致的临时展开使用程序化切换标记，不写入持久化列表；清除搜索后恢复用户原先记住的展开状态。
- 旧配置未包含展开列表时会规范化为空数组；未知、重复或失效的分类名会被过滤，避免配置污染设置页状态。
- “进入文章模式”新增“记住上次文章状态”选项，并使用独立的 `articleLastReadOnly` 保存文章模式上次的锁定/编辑状态。
- 文章状态只在文章模式内更新；导图、大纲、通读等外部视图的锁定切换不会覆盖记住的文章状态。
- 文章上次为锁定时，下次进入文章仍锁定；上次为编辑时，下次进入仍可编辑，并可跨文件、关闭设置及插件重启恢复。
- 原有“始终锁定”和“继承当前状态”策略保持兼容；旧配置会规范化到受支持的三种策略。
- 文章模式切换回其他视图时仍恢复文档原本的 `view.readOnly`，文章独立状态不会改写文件级锁定。
- 1.38.7 的缓存分帧恢复、内容块记忆和 hydration 线性索引，以及更早版本的节点局部指纹、LRU 与跨平台 ZIP 兼容继续保留。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、运行时缓存、临时目录、嵌套 ZIP，并按仓库交付规则排除本轮未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.38.8-handoff-786936.zip`。
- Codex ZIP 的中文目录和文件名使用标准 UTF-8 主文件名和 general-purpose bit 11；Linux Info-ZIP 实际解压后不存在字面量 `#Uxxxx`。
- 安装 ZIP SHA-256：`0694efede6ac6d304f0c08c092454c279893687849b684461fc63f57f22d6f86`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在 Win10 中打开设置页，确认首次进入没有任何一级分类默认展开；分别展开多个分类、关闭设置页再打开，确认恢复相同列表。
- 在设置搜索框输入关键词，确认命中分类可临时展开；清除搜索并重新打开设置页后，确认搜索过程没有污染用户记住的展开列表。
- 将“进入文章模式”设为“记住上次文章状态”，在文章模式锁定后切换到导图/大纲并改变外部锁状态，再次进入文章，确认仍保持文章锁定。
- 在文章模式切换为编辑，离开文章、切换文件或完全重启 Obsidian 后重新进入文章，确认仍保持编辑状态。
- 分别回归“始终锁定”和“继承当前状态”，确认旧行为未发生变化。
