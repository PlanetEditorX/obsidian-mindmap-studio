# Test Results

版本：1.42.4

- 单元测试：**304 / 304 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1078 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮根因

- “主题与外观”底部的“应用”按钮打开后自动获得焦点，浏览器为了显示焦点元素把弹窗滚动到最底部。
- 工具栏部分按钮重新写入原生 `title`，与 Obsidian 根据 `aria-label` 生成的黑底说明叠加。
- 任务状态功能分散在设置、工具栏、节点编辑、大纲、右键、快捷键、导图渲染、进度、SVG、Markdown 和子节点表格中。

## 本轮实现

- 取消底部按钮自动聚焦，并在弹窗布局完成后的连续两帧恢复顶部。
- 模式、缩放和普通工具按钮只保留 `aria-label`；工具栏增加动态原生提示清理边界。
- 删除所有任务状态入口、显示、快捷键和导出语义；旧 `task` 字段仅静默兼容，旧 `showTaskProgress` 设置在加载时清理。
- 保持“打开节点链接”重复命令删除，节点内链接仍可直接点击。

## 自动回归

- 验证外观弹窗不再聚焦底部按钮并两帧恢复 `scrollTop = 0`。
- 验证工具栏构建区域不再写入 `title`，动态标题也会被移除。
- 验证设置、编辑器、工具栏、快捷键和生产包均不再包含任务状态入口。

## 仍需真实 Obsidian 手工验证

- 打开“主题与外观”，确认首屏始终位于顶部。
- 逐个悬停模式按钮、普通工具按钮和缩放按钮，确认只显示一个黑底说明。
- 打开含历史任务字段的旧导图，确认不显示任务图标、进度条或任务菜单，且普通编辑保存不报错。

## 交付包验证

- 安装包 SHA-256：`b12177f40a40567675c07d80c2153cb1e2675e9c44ecd4ccfec66f662bf87da1`，已写入 `update.json`。
- 本轮交付后缀：`708559`。
