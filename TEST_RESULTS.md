# Test Results

版本：1.39.9

- 单元测试：277 / 277 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1025 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- `renderArticleMode()` 首次只挂载目标节点前后各约 5 KB 的真实章节，边缘扩展每次约 5 KB；仍无零高度占位、隐藏舞台、骨架覆盖层或后台全页 hydration。
- 向上补载会使用新增 `scrollHeight` 补偿 `scrollTop`，保持当前阅读锚点；加载前文/后文按钮与滚动边缘共用同一窗口控制器。
- 目标章节不在当前窗口时，`ensureNode()` 会先围绕目标重建窗口，定位只匹配精确文章标题或章节节点。
- 同一物理文件的目录项直接调用 `focusNode()`，先切换纯目录页到正文，不再重新打开当前文件。
- 显式章节位置由本轮文章重绘独占，旧阅读位置不会通过 20 ms 或双帧恢复覆盖新目标。
- 大文件章节权重读取原始规范化字段，不再为全部节点预先规范化内容块或执行整节点 `JSON.stringify()`。
- 父级返回仍统一携带 `parentPath + parentNodeId`，缺失节点 ID 时按父导图子导图路径反查。

交付包验证：

- 源码 ZIP 第一层固定为 `obsidian-mindmap-studio/`，安装 ZIP 固定为 `mindmap-studio/`，Codex ZIP 固定为 `Codex/`。
- 三份 ZIP 的非 ASCII 路径使用 UTF-8 主文件名与 general-purpose bit 11，并执行实际解压验证。
- 安装包 SHA-256：`cbaaa415d64b4bd3adc4d6c94032d1bef503a508a4559d7e988ec2a038bbec85`，已写入 `update.json`。
- 本轮交付后缀：`456047`。

仍需真实 Obsidian 手工验证：

- 在 80 KB 以上、包含图片/代码/表格的文章中点击远端章节，确认首屏快速出现且 Obsidian 不长时间无响应。
- 连续向下和向上滚动跨越多个 5 KB 窗口，确认只在边缘扩展，向上补载不改变当前段落屏幕位置。
- 从顶层目录点击当前文件章节、跨文件深层章节，并快速连续点击两个不同条目，确认最终停在最后一次点击的精确章节。
- 从子导图分别使用底部分页、顶部父级导航和 `Esc` 返回，确认回到父挂载节点。
