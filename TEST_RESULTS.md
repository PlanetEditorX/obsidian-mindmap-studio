# Test Results

版本：1.38.9

- 单元测试：273 / 273 通过
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过（53 个源码模块、1051 个具名声明）
- 仓库检查：通过
- 生产构建：通过，`main.js` 已重新生成

本轮专项验证：

- 连续三个待保存版本会合并为一轮最新快照写入，三个调用方均在该快照落盘后完成。
- 写入进行期间到达的新版本会在当前写入完成后立即追加；任意时刻最多一个持久化回调运行，旧写入不会晚于新写入完成并覆盖最新配置。
- 一轮写入失败时对应等待方会收到错误；后续显式保存可以用当前最新内存状态恢复，不需要重启插件。
- 设置保存仍保留 `await` 行为，插件卸载会刷新待写版本；卸载期间不会再安排文件浏览器 DOM 扫描。
- 文件浏览器 MutationObserver 只在变化发生于文件浏览器内部、或新增/移除文件浏览器容器时安排扫描；文章渐进渲染和其他工作区 DOM 变化被忽略。
- 文件后缀、隐藏目录和资源目录规则每轮只规范化一次；规则签名忽略大小写、分隔符、排序等格式差异。
- 10,000 条路径、30 个后缀与 30 个目录规则的本地专项基准：旧逐条解析约 119.6 ms，新预编译谓词约 9.5 ms，当前环境约 12.6 倍提升。
- 1.38.8 的设置分类展开记忆和文章独立锁状态继续保留，并统一经过新的串行合并保存链路。

交付包验证：

- 源码 ZIP 第一层目录固定为 `obsidian-mindmap-studio/`，排除 `node_modules/`、`.git/`、运行时缓存、临时目录、嵌套 ZIP，并按仓库交付规则排除本轮未修改的 `examples/`。
- 安装 ZIP 第一层目录固定为 `mindmap-studio/`，只包含 `main.js`、`manifest.json` 和 `styles.css`。
- Codex ZIP 第一层目录固定为 `Codex/`，外部文件名严格使用 `Codex-1.38.9-handoff-203041.zip`。
- Codex ZIP 的中文目录和文件名使用标准 UTF-8 主文件名和 general-purpose bit 11；Linux Info-ZIP 实际解压后不存在字面量 `#Uxxxx`。
- 安装 ZIP SHA-256：`e5347b037904d87f2b683d6af4c86adb25a8b46e23a7b21d4615cf5066899510`，已写入 `update.json`。

仍需真实 Obsidian 手工验证：

- 在 Win10 中快速连续输入 AI/图床等长文本设置、展开和收起多个设置分类，立即关闭并重启 Obsidian，确认最后输入与最后展开列表完整保留。
- 快速切换文章锁定/编辑状态并离开文章模式，重启后确认“记住上次文章状态”仍恢复最后一次状态。
- 打开大型文章并让渐进渲染持续进行，同时展开文件浏览器目录，确认文件浏览器不闪烁、筛选仍正确，文章渲染不会反复触发文件树扫描。
- 修改隐藏后缀、隐藏目录和资源目录开关，确认筛选立即更新；只改变输入格式但语义相同时不应出现额外闪烁。
