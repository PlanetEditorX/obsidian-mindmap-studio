# Test Results

版本：1.42.3

- 单元测试：**301 / 301 通过**
- 综合回归：通过
- TypeScript 类型检查：通过
- 文档覆盖检查：通过，**55 个源码模块、1086 个具名声明**
- 仓库检查：通过
- 生产构建：通过

## 本轮根因

- 通读目录按整本父子导图生成编号，但子导图中心节点的关闭状态没有沿挂载关系传给该物理导图的后代，因此主标题可以无编号，下面章节仍继续显示“第一节”等标签。
- 通读正文先向标题容器插入编号，再把富文本标题渲染到同一容器；富文本渲染会先清空容器，刚插入的编号随即被删除。
- 通读正文还会按单个物理导图重新计算层级，和全书目录的跨文件层级来源不一致。

## 本轮实现

- 文章族遍历为每个 `ReadingSection` 记录并继承 `numberingDisabled`；顶层或子导图中心节点关闭后，该挂载子树的章节和末端序号全部关闭。
- 通读正文按 `filePath + nodeId` 复用全书目录的层级、标题和编号。
- 编号与富文本标题改为独立 DOM 子元素，富文本渲染只更新标题文字，不再删除编号。
- 文章子页及 HTML、Markdown、Word 导出使用同一继承关闭状态。

## 自动回归

- 验证中心节点关闭编号后，本导图全部标题和末端序号为空，结构深度保持不变。
- 验证祖先关闭状态可抑制挂载子导图中的章节和末端序号。
- 验证普通节点关闭仍只跳过自身，其后代继续编号。
- 验证通读目录与正文复用同一跨文件编号结果。
- 验证富文本标题渲染不会清除编号元素。
- 验证文章子页和导出继承祖先关闭状态。

## 仍需真实 Obsidian 手工验证

- 在顶层导图的通读模式中切换自动、手动层级和关闭，确认全书目录与正文同时变化。
- 在某个子导图中心节点关闭编号，确认该章主标题、下面的“第一节”等章节和末端序号全部隐藏，其他兄弟子导图不受影响。
- 恢复自动或手动编号，确认目录和正文同步恢复，并保持当前阅读位置。
- 检查对应子文章页与 HTML、Markdown、Word 导出编号一致。

## 交付包验证

- 安装包 SHA-256：`9957883a923dbe5c314a6bcaf721b85aebaa5f7849cf1b4a0bd61005d0e85e69`，已写入 `update.json`。
- 本轮交付后缀：`756557`。
